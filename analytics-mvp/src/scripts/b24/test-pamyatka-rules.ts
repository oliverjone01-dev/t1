// Мутационные фикстуры движка куратора (G11 ФЕНИКСА): синтетическая переписка
// с известным правильным ответом. Любой дрейф правил валит прогон.
// Запуск: npx tsx src/scripts/b24/test-pamyatka-rules.ts (smoke-svod2 зовёт через execSync)
import { kuratorAudit, workMinutes } from "./pamyatka-rules";

const T0 = new Date("2026-08-25T11:00:00+03:00").getTime(); // рабочее утро
const ev = (min: number, dir: string, body: string, who = "Продавцова Анна", dealId = "1") => ({
  ts: String(T0 + min * 6e4), dt: new Date(T0 + min * 6e4).toISOString().replace("Z", "+03:00").slice(0, 19) + "+03:00",
  dealId, leadId: "", dir, who, body, mgr: "Продавцова Анна",
});
const dlg = {
  from: "2026-08-23T00:00:00+03:00", to: "2026-08-30T23:59:59+03:00",
  events: [
    // сделка 1: клиент пишет, продавец отвечает через 40 РАБОЧИХ минут -> R1-улика, fr=1, frOk=0
    ev(0, "входящее", "Здравствуйте, посчитайте перегородку", "Клиент"),
    ev(40, "исходящее", "Добрый день! Посчитаю и пришлю расчёт", "Продавцова Анна"), // и R2: обещание без даты
    // маркер вложения без текста рядом -> R3 (одна улика, батч минуты)
    ev(60, "исходящее", "Отправлено Файл", "Продавцова Анна"),
    ev(60.5, "исходящее", "Отправлено Изображение", "Продавцова Анна"),
    // «в ближайшее время» без обещания-глагола в том же предложении -> R5
    ev(90, "исходящее", "Ответ будет в ближайшее время", "Продавцова Анна"),
    // сделка 2: маркер вложения НЕ считается обещанием; «уточните» (просьба) не обещание;
    // вежливая формула не обещание; обещание С датой -> prOk
    ev(0, "входящее", "Добрый день", "Клиент", "2"),
    ev(5, "исходящее", "Добрый день! С радостью отвечу на вопросы", "Продавцова Анна", "2"),
    ev(6, "исходящее", "Уточните, пожалуйста, размеры", "Продавцова Анна", "2"),
    ev(7, "исходящее", "Пришлю расчёт завтра до 12:00", "Продавцова Анна", "2"),
    ev(8, "исходящее", "Отправлено Файл", "Продавцова Анна", "2"),
    ev(9, "исходящее", "Направляю КП по вашему запросу, посмотрите пожалуйста", "Продавцова Анна", "2"),
    // сделка 3: письмо-тред с «руководителем» в цитате -> НЕ эскалация; звонок не «клиент написал»
    ev(0, "входящее", "Звонок входящий, 0:45", "Клиент", "3"),
    ev(10, "входящее", "Тема: Re: запрос\nНаш руководитель просил уточнить смету, цитирую переписку ниже", "Клиент", "3"),
    ev(20, "исходящее", "Смету уточню и пришлю сегодня до 18:00", "Продавцова Анна", "3"),
    // сделка 4: ответ писал ОФИСНЫЙ сотрудник -> продавцу не атрибутируется, идёт в первую линию
    ev(0, "входящее", "Посчитайте стол", "Клиент", "4"),
    ev(200, "исходящее", "Передала менеджеру, посчитает и пришлёт", "Офисова Ольга", "4"),
    // сделка 6: продавец пишет под ВТОРЫМ написанием имени («Анна Продавцова») - копилка одна
    ev(0, "входящее", "Нужен стеллаж", "Клиент", "6"),
    ev(30, "исходящее", "Стеллаж посчитаю и пришлю", "Анна Продавцова", "6"),
    // сделка 7: G3-регрессы - текст 20 знаков в ту же минуту -> R3 НЕТ; и за 8 минут -> R3 НЕТ
    ev(0, "входящее", "Пришлите образец", "Клиент", "7"),
    ev(5, "исходящее", "Вот образец берёзы", "Продавцова Анна", "7"),
    ev(5.5, "исходящее", "Отправлено Изображение", "Продавцова Анна", "7"),
    ev(20, "исходящее", "Ещё вариант из ясеня для сравнения", "Продавцова Анна", "7"),
    ev(27, "исходящее", "Отправлено Файл", "Продавцова Анна", "7"),
    // сделка 8: НОВЫЙ диалог, никто не поздоровался ни разу -> R10
    ev(0, "входящее", "Сколько стоит зеркало 2 метра", "Клиент", "8"),
    ev(5, "исходящее", "Зависит от рамы, пришлите фото проёма пожалуйста?", "Продавцова Анна", "8"),
    // сделка 10: ДОМИНИРУЮЩИЙ поток - менеджер поздоровался ПЕРВЫМ, клиент ответил,
    // менеджер продолжил по делу -> R10 НЕТ (урок дельта-аудита: 29 из 37 ложных были этим)
    ev(0, "исходящее", "Юрий, добрый день! Меня зовут Анна, компания GENGLASS. По вашему запросу стола готовлю расчёт", "Продавцова Анна", "10"),
    ev(10, "входящее", "Добрый, жду цифры", "Клиент", "10"),
    ev(15, "исходящее", "Стол по таким параметрам от 60 000 руб, отправляю варианты сегодня до 17:00", "Продавцова Анна", "10"),
    // сделка 11: первый «ответ» - маркер вложения -> R10 не выставляется на маркер
    ev(0, "входящее", "Пришлите каталог", "Клиент", "11"),
    ev(3, "исходящее", "Отправлено Файл", "Продавцова Анна", "11"),
    // сделка 9: у сделки есть история ДО окна - середина переписки, приветствие не требуется
    { ts: String(new Date("2026-08-10T12:00:00+03:00").getTime()), dt: "2026-08-10T12:00:00+03:00", dealId: "9", leadId: "", dir: "исходящее", who: "Продавцова Анна", body: "Добрый день! Начали обсуждение", mgr: "Продавцова Анна" },
    ev(0, "входящее", "Ну что там с расчётом", "Клиент", "9"),
    ev(5, "исходящее", "Расчёт готов, отправляю сегодня до 14:00", "Продавцова Анна", "9"),
    // сделка 12: опечатка «Нижу» = «Ниже» (подтверждена Иваном 04.09, разметка) - НЕ улика
    ev(0, "входящее", "Какая фурнитура подойдёт?", "Клиент", "12"),
    ev(4, "исходящее", "Добрый день! Нижу пришлю фурнитуру, которую подбирал Карен", "Продавцова Анна", "12"),
    // сделка 13: обещание, обусловленное действием клиента, - не улика (вторая
    // спорная R2 разметки Ивана 04.09: «выбирайте и посчитаю»)
    ev(0, "входящее", "Покажите варианты плёнок", "Клиент", "13"),
    ev(6, "исходящее", "Добрый день! Также пленок мдф множество, выбирайте и посчитаю, впишем в ваш интерьер", "Продавцова Анна", "13"),
    // событие ВНЕ окна -> не считается вовсе
    { ts: String(new Date("2026-06-01T12:00:00+03:00").getTime()), dt: "2026-06-01T12:00:00+03:00", dealId: "5", leadId: "", dir: "исходящее", who: "Продавцова Анна", body: "Пришлю расчёт когда-нибудь", mgr: "Продавцова Анна" },
  ],
};
const lookup = (k: string) => ({ dealId: k.slice(1), isLead: 0, mgr: "Продавцова Анна", title: "Тест " + k, budget: 100000 });
// B1 ФЕНИКСА: канонизация - «Анна Продавцова» и «Продавцова Анна» это ОДИН человек
const ROSTER = ["Продавцова Анна"];
const tokset = (x: string) => new Set(x.toLowerCase().split(/\s+/).filter(Boolean));
const same = (a: string, b: string) => { const ta = tokset(a), tb = tokset(b); if (ta.size !== tb.size || !ta.size) return false; for (const t of ta) if (!tb.has(t)) return false; return true; };
const canon = (n: string) => ROSTER.find((x) => same(x, n)) || null;
const K = kuratorAudit(dlg as any, lookup, (s) => s, canon);

let fails = 0, total = 0;
const ck = (name: string, ok: boolean, info = "") => { total++; if (ok) console.log("OK  ", name); else { fails++; console.log("FAIL", name, info); } };
const A = K.mgrs["Продавцова Анна"] || ({} as any);
const allV = (A.deals || []).flatMap((d: any) => d.viol.map((v: any) => v.r + "|" + v.q));
ck("R1: 40 рабочих минут - улика есть", allV.some((v: string) => v.startsWith("R1|")));
ck("R1: рабочие минуты в формулировке", allV.some((v: string) => v.startsWith("R1|") && v.includes("рабочего времени")));
ck("R2: «посчитаю и пришлю» без даты - улика", allV.some((v: string) => v.startsWith("R2|") && v.includes("Посчитаю и пришлю")));
ck("R2: маркер вложения НЕ обещание", !allV.some((v: string) => v.startsWith("R2|") && v.includes("Отправлено")));
ck("R2: «уточните» (просьба клиенту) не обещание", !allV.some((v: string) => v.includes("Уточните")));
ck("R2: вежливая формула не обещание", !allV.some((v: string) => v.includes("С радостью")));
ck("R2: «направляю» (уже сделанное) не обещание", !allV.some((v: string) => v.includes("Направляю КП")));
ck("R2: обещание с датой прошло в prOk", A.prOk >= 2, "prOk=" + A.prOk);
ck("R3: батч вложений минуты = одна улика на сделку (d1 батч + d11 одиночный = 2)", allV.filter((v: string) => v.startsWith("R3|")).length === 2, JSON.stringify(allV.filter((v: string) => v.startsWith("R3|"))));
ck("R3: вложение с текстом-соседом (сделка 2) не улика", !allV.some((v: string) => v.startsWith("R3|") && v.includes("Тест d2")));
ck("R5: «в ближайшее время» - улика", allV.some((v: string) => v.startsWith("R5|")));
ck("R9: «руководитель» в письме-треде НЕ эскалация", !allV.some((v: string) => v.startsWith("R9|")));
ck("Звонок и письмо-тред не считаются «клиент написал» (сделка 3 пары не даёт: пары d1,d2,d6-d13)", A.fr === 10, "fr=" + A.fr);
ck("Офисный автор: улика НЕ у продавца, пара в первой линии", K.firstLine.fr === 1 && !allV.some((v: string) => v.includes("Передала менеджеру")), "flFr=" + K.firstLine.fr);
ck("Событие вне окна не считается", !allV.some((v: string) => v.includes("когда-нибудь")));
ck("workMinutes: ночь не считается", workMinutes(new Date("2026-08-25T17:50:00+03:00").getTime(), new Date("2026-08-26T09:10:00+03:00").getTime()) <= 25, String(workMinutes(new Date("2026-08-25T17:50:00+03:00").getTime(), new Date("2026-08-26T09:10:00+03:00").getTime())));
ck("workMinutes: суббота не считается (N1)", workMinutes(new Date("2026-08-29T10:00:00+03:00").getTime(), new Date("2026-08-29T11:00:00+03:00").getTime()) === 0, String(workMinutes(new Date("2026-08-29T10:00:00+03:00").getTime(), new Date("2026-08-29T11:00:00+03:00").getTime())));
ck("B1: два написания имени - одна копилка", Object.keys(K.mgrs).length === 1 && !K.mgrs["Анна Продавцова"], JSON.stringify(Object.keys(K.mgrs)));
ck("B1: улика второго написания легла в канон", allV.some((v: string) => v.includes("Стеллаж посчитаю")));
ck("G3-регресс: текст 20 знаков в ту же минуту гасит R3", !allV.some((v: string) => v.startsWith("R3|") && v.includes("Изображение") && v.includes("d7")) && !K.mgrs["Продавцова Анна"].deals.some((d2: any) => d2.t === "Тест d7" && d2.viol.some((x: any) => x.r === "R3" && x.q.includes("Изображение"))));
ck("G3-регресс: текст за 8 минут гасит R3 (окно 10 мин)", !K.mgrs["Продавцова Анна"].deals.some((d2: any) => d2.t === "Тест d7" && d2.viol.some((x: any) => x.r === "R3" && x.q.includes("Файл"))));
ck("Неопознанный автор не теряется: 10 пар продавца + 1 первой линии = 11 всего", K.firstLine.fr === 1 && K.mgrs["Продавцова Анна"].fr === 10, "fl=" + K.firstLine.fr + " fr=" + K.mgrs["Продавцова Анна"].fr);

ck("Приветствие - СЧЁТЧИК, не улика (решение ФЕНИКСА): улик R10 нет вовсе", !allV.some((v: string) => v.startsWith("R10|")));
// якорь счётчика - ПЕРВОЕ исходящее нового диалога: новые d1,d2,d3,d6,d7,d8,d10 = 7
// (d9 - история до окна, d11 - только маркер, d4 - офисный автор)
ck("hiN: новых диалогов с первым сообщением продавца = 9", A.hiN === 9, "hiN=" + A.hiN);
ck("hi: поздоровались в первом сообщении d1,d2,d10,d12,d13 = 5", A.hi === 5, "hi=" + A.hi);
ck("qe: счётчик «вопрос в конце» считает (одно сообщение с ? в фикстурах)", A.qe === 1, "qe=" + A.qe);
if (fails) { console.error("PAMYATKA-RULES TEST: " + fails + " FAIL из " + total); process.exit(1); }
if (total < 20) { console.error("PAMYATKA-RULES TEST: проверок " + total + " < 20 - кто-то удалил фикстуры"); process.exit(1); }
console.log("PAMYATKA-RULES TEST PASS: " + total + "/" + total);
