# GG-SEO/GEO Монстр

Отдельный продукт GENGROUP: движок органической и AI-видимости для 5 брендов
(GENGLASS, VALONTI, GENTERO, Metal-GM, GLASS-MEMORY), **приоритет GENGLASS**.

Хостинг (решение Ивана 07.07.2026): пока на GitHub Pages Ивана, подпапка
`/seo/` в общем сборщике `deploy-pages.yml`. Отдельный домен - позже, отдельным
решением, не блокирует запуск.

Владелец домена задачи: **semyon** (SEO/AEO/GEO). Гейт перед публикацией -
**feniks** (Step 12.5). Данные по цифрам - **data** (Protocol 9). Приёмка
статей - **София** (контент-менеджер).

## Зачем это отдельный проект, а не папка в аналитике

- **Другая аудитория.** Аналитика (`analytics-mvp/`) - для коммерческого блока
  (P&L, воронка OZON). Этот проект - для контент- и SEO-команды: кластеры,
  публикации, цитируемость в AI-поиске.
- **Свой раздел на сайте.** Хаб публикуется в `/seo/` на GitHub Pages Ивана,
  изолированной статикой, а не мешается в аналитику. Переезд на отдельный
  домен - позже, без переписывания проекта (просто смена базового URL).
- **Другой цикл.** SEO меряется неделями и месяцами, аналитика МП - днями.

## Что внутри (модули)

| Модуль | Статус | Что делает |
|---|---|---|
| **Content Forge** | есть промпт, нет UI | Кластер ключей → статья (6 блоков: META, текст, промпты картинок, Schema.org, GEO-чеклист, перелинковка). Источник: `../SEO_PIPELINE_content_forge_prompt.md` |
| **GEO/AEO Checklist Engine** | скилл готов | 7-точечная проверка цитируемости в ChatGPT / Perplexity / YandexGPT / Gemini. Источник: скилл `geo-aeo` |
| **AI Citation Tracker** | план | Замер AI-видимости по KPI: Prompt Win Rate, Share of Model, Coverage, Answer Share, AI Visibility Score (определения - `knowledge/geo-intel-intake.md`). Baseline → динамика |
| **Schema.org Generator** | план | Article + FAQPage + Product + BreadcrumbList JSON-LD под вставку в `<head>` |
| **Keyword Clusters** | план | Карта кластеров pillar/satellite с перелинковкой |
| **Rank Monitor** | план | ТОП Яндекс / Google по кластерам, без розовых очков (реальные позиции) |

## Структура папки

```
gg-seo-geo-monster/
  README.md              # этот файл - charter проекта
  SETUP.md               # единственный ручной шаг владельца: 4 секрета
  DECISIONS.md           # решения автономной сборки GEO-monster v1.0
  BACKLOG.md             # что дальше, по приоритету
  ROADMAP.md             # этапы, Kill Criteria
  public/                # приложение GEO-monster (статический SPA, без сборки)
    index.html           # оболочка: гейт по паролю + роутер
    app.js / app.css     # ядро: дашборд, семантика, GEO-трекер, отчёты, админка
    forge.js             # Content Forge + внешние площадки + image-промты
    about.html           # манифест проекта (бывший лендинг)
    intel.html           # разведка GEO рунета (18 источников)
    demo/                # DEMO-снапшоты (та же схема, что data/)
  config/projects.json   # предзаведённые проекты: genglass.ru, glass-memory.ru
  scripts/
    harvest.mjs          # ночной сбор: keys.so + Метрика + Директ -> data/
    gen-demo.mjs         # генератор DEMO-данных
    keyso.mjs            # разведочный сборщик keys.so (сырые отчёты)
    wordstat.mjs         # сборщик частот Wordstat
  data/                  # живые снапшоты (коммитит GitHub Actions, не руками)
  docs/                  # спеки интеграций (keys.so, Wordstat)
  knowledge/             # приёмки экспертизы, канон KPI, разведотчёты
  content-runs/          # ручные прогоны контент-конвейера с гейтами feniks
```

Вход в хаб - по паролю (дефолт `genmonster2026`, смена через секрет `HUB_PASS` -
см. SETUP.md). Это защита от случайных глаз, не криптография: хэш пароля лежит в
клиентском коде, репозиторий публичный. Ничего чувствительного в data/ не хранится -
только агрегированные SEO-метрики.

## Как запустить локально

Статика без сборки. Любой статический сервер из папки `public/`:

```bash
cd gg-seo-geo-monster/public
python3 -m http.server 8080
# открыть http://localhost:8080
```

## Деплой (GitHub Pages, подпапка /seo/)

Хостинг настроен в общем сборщике `.github/workflows/deploy-pages.yml` по образцу
`/smm/`: статика `public/` копируется в `_site/seo/` при push в `main` (пути
`gg-seo-geo-monster/**`). Отдельный домен - позже, отдельным решением.

- **Боевой URL после мерджа в main:** `https://<user>.github.io/<repo>/seo/`
- Публикуется только с ветки `main`. Пока проект на ветке PR - в бой не выезжает.
- Перед первым публичным деплоем - Step 12.5 (feniks) и HITL Ивана (Protocol 6).

## Данные из Google (контент-план и статьи)

- **Контент-план:** единая Google-таблица (владелец Иван). Импорт в хаб - по
  образцу существующих воркфлоу репо (`competitors-import.yml`, `card-groups-import.yml`):
  читаем CSV-экспорт вкладки, auth не нужен, таблица открыта «по ссылке - Читатель».
- **Статьи:** черновики в Google Docs, приёмку ведёт София.
- Нужные доступы Ивана перечислены в `ROADMAP.md` (Этап 1).

## Границы (чего проект НЕ делает)

- Не публикует контент сам - это делает контент-конвейер (n8n Content Forge)
  после ревью. Хаб только показывает состояние и даёт инструменты.
- Не заменяет `analytics-mvp/` - там деньги и продажи, здесь видимость.
- Цифры цитируемости и позиций подаются как замеры с датой и источником,
  а не как прогноз. Прогноз без источника = блок (Protocol 9).
