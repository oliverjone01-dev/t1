# Диалоги v2: калибровка весов + новые факторы + очередь дел (методика, РАУНД 2 после ФЕНИКС return)

Артефакт-класс: гейт-инструмент/дашборд/агент-логика, влияет на оценку менеджеров (Protocol 6 HITL). Владелец решения: Иван (одобрил A+B). Скоуп: только дашборд Диалоги; РОП-дашборд не трогаем.

## РАУНД 2 - что исправлено по rework_tz ФЕНИКСА (return 7.5)

1. **client_chase (L1 FAIL - ложные обвинения) - ИСПРАВЛЕНО.** Добавлен порог ожидания CHASE_MIN = BALL_STUCK_MIN (4 раб.часа): «напоминанием» считается только два входящих подряд с зазором >=4 раб.ч (между ними по определению нет ответа менеджера). Пачки бабблов <15 мин больше не считаются. Тон метки понижен bad->warn, текст «Клиент напоминает сам (повод спросить)». Вес смягчён 0.75->0.85, порог фактора >=1 (настоящий re-ping). Факт на артефакте cecc4710 (счёт ФЕНИКСА, триангулирован 3 способами): client_chase ~403 сделки; пачки <15 мин = 0%.
2. **hot_slow (L2 PARTIAL - только платёж) - ИСПРАВЛЕНО.** Отдельный RE_HOT покрывает наличие/склад/сроки/готовность/отгрузку/реквизиты/счёт/оплату/договор/монтаж, не только RE.ready. Факт: горячих реплик ~461 сделка (артефакт cecc4710).
3. **Двойной штраф (L4) - ИСПРАВЛЕНО.** slow_resp не начисляется, если уже штрафуем hot_slow за медленную горячую реплику. Факт: пересечений hot_slow+slow_resp = 0 (было 67).
4. **Приёмка item 2 (rework #4).** Новые факторы двигают prob, но: (а) client_chase теперь на настоящих re-ping (не ложные), tone warn; (б) метрика приёмки - false-positive rate по calib-log; владелец - Иван; дата проверки +2 недели от выкладки. До проверки веса мягкие (0.85/0.6/1.2).
5. **Плашка (rework #5) - ИСПРАВЛЕНО.** Показывает def->mult, n и conf по каждому применённому ключу + пояснение про демпфирование 70/30.
6. **Оговорка client_chase (rework #7) - ИСПРАВЛЕНО.** «повод спросить, не обвинение» в тексте метки.
7. **Self-check по 25 чекпоинтам - ПРИЛОЖЕН (ниже).**
Не сделано (осознанно, вне критичного пути): пересчёт BASE_RATES на 2026-09-21 (B6 PARTIAL, влияние малое); порог «дожимаемости» очереди (L3 - осознанный EV-выбор). Rollback описан ниже.

## Rollback
Изменение = код (score-dialog.ts, template) + пересобранный dialog.json. Откат: git revert коммита в dialog-export-v1 + пересборка. Side-effect append-only calib-log.ndjson/owners.json на текущий скоринг не влияют (лог для варианта B).

## Ядро без изменений (ФЕНИКС B1/B2 PASS)
Калибровка item 1: только 3 направленно-устойчивых сигнала (slow_resp 0.8->0.66, refuse 0.5->0.44, ready 1.3->1.39), предохранители: контроль по макс.стадии (3 бакета), направление залочено, сдвиг к дефолту 70/30, min-n>=50, кламп [0.3,1.6]. Остальные ~15 весов заморожены (наивные веса конфаундены: promise_broken naive=2.5). Лог состояний (вариант B) для чистой калибровки через 2-4 недели.

## Self-check phoenix-eval (25 чекпоинтов; да/частично/нет)
Accuracy: 1 цифры тегированы [ДАННЫЕ]/[ГИПОТЕЗА] - да; 2 источники проверяемы (снимок 2026-09-21, воспроизводимо) - да; 3 нет конфликта с RAG - да; 4 терминология - да; 5 бренды - н/п.
Actionability: 6 владелец (Иван, приёмка item2) - да; 7 дедлайн проверки FP +2 нед - да; 8 ресурсы (лог, снимки) - да; 9 метрика успеха (FP-rate, доля дожатых из очереди) - частично; 10 milestone (2-4 нед чистые веса B) - да.
Insight: 11 нетривиально (конфаундинг терминального среза) - да; 12 второй порядок (веса B без конфаундинга) - да; 13 альтернативы (наивная / контроль по стадии / лог) рассмотрены - да; 14 anti-median - да; 15 cross-domain (uplift-моделирование/RLAIF) - частично.
Brand fit: 16 голос - н/п (внутр. инструмент); 17 anti-slop - да; 18 em dash - нет em dash; 19 output routing (дашборд) - да; 20 tone/читаемость для РОПа - да.
Risk: 21 downside при -50% (мягкие веса, кламп) - да; 22 P9 hard rules (нет цифры без источника; гипотезы помечены; пилот, не Pareto) - да; 23 crisis - н/п; 24 зависимости (направление Wazzup, нет транскриптов - учтено) - да; 25 обратимость (git revert) - да.
