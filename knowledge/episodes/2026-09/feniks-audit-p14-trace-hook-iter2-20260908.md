# ФЕНИКС audit - хук P14 subagent-trace, итерация 2 (2026-09-08)

**thread_id:** feniks-2026-09-08T09:40Z-p14-trace-hook · **iteration:** 2 · **класс артефакта:** гейт / хук
**deliverable_ref:** `git diff origin/main -- .claude/hooks/subagent-trace.sh .claude/hooks/tests/run-hook-tests.sh .claude/hooks/tests/scope-cases.tsv .claude/skills/reflexion/scripts/trace-summary.py schemas/agent-trace.json .claude/skills/roster-protocol/SKILL.md CLAUDE.md knowledge/episodes/2026-09/p14-trace-hook-roster-filter-20260908.md`
**self-check автора:** приложен (`p14-trace-hook-roster-filter-20260908.md` §5) - скоринг запущен.
**JSON:** `knowledge/episodes/2026-09/feniks-audit-p14-trace-hook-iter2-20260908.json`, `validate.py audit-report` -> `VALID`, пересчёт весов сходится (дельта 0.00).
**Итерация 1:** 7.7 return, A6 FAIL (`knowledge/episodes/2026-09/feniks-audit-p14-trace-hook-20260908.md`).

## VERDICT: go · weighted_total 7.95 · CONFIDENCE 0.86

Семь пунктов rework_tz итерации 1 закрыты проверяемо: цифры §1 воспроизведены до единицы, whitelist префикса
отбивает все восемь вариантов подделки имени через `:`, регрессионный тест ловит пять мутаций из шести.
Уходит в `go` с восемью gaps, потому что остаточные дефекты наблюдаемы и откатываемы: главный из них не «система
молчит», а «система ещё не показала главное». Понижения по Rule 9 нет: FAIL стоит на мутационной пробе M9
(дыра в тесте, не в гейте), пробы класса A дали PASS и два PARTIAL.

## anchor

`anchor: 8.5 agent-timur-activation iter3 (класс гейт) - наш артефакт ниже на 0.55`. Механика та же, что вывела
ТИМУРА на 8.5: whitelist вместо суффиксного разбора, env только расширяет, live-пробы в обе стороны. Ниже якоря
по двум причинам: главный эффект правки на реальном событии харнесса ещё не наблюдался (у ТИМУРА гейт был проверен
живой атакой на реальном вызове API), и ось суффикса `@` / ` (` осталась без whitelist, то есть тот же класс дефекта
закрыт наполовину. Ближайший снизу - 7.9 timur iter2 (там дыра была открыта, здесь закрыта, поэтому +0.05).
Расхождение с якорем 0.55, дрейфа нет.

## Red-team probes

| ID | Result | Evidence |
|---|---|---|
| A6 wildcard scope | PARTIAL | 22 вектора. Отбито: `evil-plugin:feniks`, `output-style:marco`, `x:y:data`, `:feniks`, `feniks:`, `evil:gengroup-roster:feniks`, `gengroup-roster:evil:feniks`, `gengroup-rosterX:feniks`. Пропущено по whitelist: `gengroup-roster:feniks`, `GENGROUP-ROSTER:feniks`, `gengroup-roster : feniks`, `gengroup-roster:FENIKS`, `gengroup-roster:fenIks@synced (plugin)`. Остаток: `feniks@evil-plugin` и `feniks (evil-plugin)` -> `{agent: feniks, tier: 0, feniks_score}` |
| A5 мёртвый гейт | PASS | 13/13 ростера пишут строку с верным tier; 8 чужих имён и 5 чужих `hook_event_name` -> строки нет. Живое подтверждение: транскрипт `aec344d99ebf9d957` (Explore, создан 09:51:44, после правки 09:29) строки не дал, а `a475220d9c044754d` (data, 09:51:42) дал start и stop. До правки Explore строки получал (журнал 09:26:33 и 09:26:37) |
| A3 env-override | PASS | 8 значений `P14_TRACE_NONROSTER` не убирают строку ростера; `=1` только расширяет и чужой префикс пишет как `unknown`, не как `feniks`; пять кандидатов в выключатели не влияют |
| A8 сшивка (наш вектор) | PARTIAL | Чужой start (`explore`) личность не одалживает; start ростера сшивает при пустом значении и при отсутствии ключа; чужой префикс не сшивается никогда. Поведение верное, guard тестом не защищён (см. M9) |
| D4 схема выхода | PASS | `traces/2026-09-08/agents.jsonl` 24/24 valid; `agent: unroutable` проходит `^[a-z][a-z0-9-]*$`; мутация M10 ловится строкой `[trace-schema]` |
| M5 снят whitelist | PASS | 138/142, FAIL на t-e1, t-e2, t-e5, t-e4 |
| M6 снят `has_type_key` | PASS | 141/142, FAIL на t-none3 |
| M7 `unroutable` снова молчит | PASS | 136/142, 6 FAIL |
| M8 вердикт нераспознанному | PASS | 139/142, 3 FAIL |
| M9 сшивка по любому start | **FAIL** | 142/142 passed: guard `prev.get("agent") in TIER` (`subagent-trace.sh:72`) мутацией не ловится. Тот же класс: M11 (снято `note: norm:<status>`) -> 142/142 |
| M10 `agent` в верхнем регистре | PASS | 122/142, включая `[trace-schema]` |
| H1 реплей payload харнесса | PARTIAL | Пустое значение / None / пробелы -> строки нет (цель достигнута); `Task Tool`, `General Purpose`, `internal_subagent`, `2`, отсутствие ключа -> строка `unroutable` на каждое событие |
| A1, A2, A4, A7 | N/A | не approval-гейт, команд не матчит, срока действия у строки нет |

Базовый прогон: `bash .claude/hooks/tests/run-hook-tests.sh` -> `hook tests: 142/142 passed, 0 failed` (11 с).
Заявленное автором число подтверждено. Мутации гонялись на копии `.claude/hooks` + `schemas` в `/tmp`, продукт не менялся.

## Наблюдение за живым журналом (зона внимания из брифа)

С 09:29 (mtime правки) по 10:21 UTC в `traces/2026-09-08/agents.jsonl` ноль строк `unroutable`
(`grep -c unroutable` = 0 по всем трём дням сентября). Строка на каждый ход не появилась: за это время журнал
пополнился только строками ростера (data, feniks) и строками `gate` от другого хука. Но и обратное не доказано:
предыдущее внутреннее событие было в 09:16:27, интервалы 08.09 составляли 61-65 минут, то есть следующее ожидалось
около 10:18, а главная сессия с 09:52 занята этим аудитом и почасовой цикл, судя по истории коммитов
`chore(traces)`, привязан к её ходу. Вывод: канал `unroutable` пока не стал источником строки на каждый ход,
но решающее наблюдение возможно только после возврата этого аудита. Это gap 2 и rework п.1, не «проверено».

## Evidence ledger

| # | Gap | Evidence | Чекпоинт | Вес |
|---|---|---|---|---|
| 1 | Whitelist поставлен только на префикс до `:`; суффиксы `@...` и ` (...)` срезаются без проверки | фикстура: `feniks@evil-plugin`, `feniks (evil-plugin)` -> строка `{agent: feniks, tier: 0, verdict: go, feniks_score: 9.9}`; `evil-plugin:feniks` в тех же условиях -> строки нет. Место: `subagent-trace.sh:58` | risk_25 | -1 |
| 2 | Главный эффект правки на реальном событии не наблюдался | `traces/2026-09-08`: внутренние события 00:52:44 … 09:16:27, интервалы 62.1 / 61.5 / 62.7 / 61.2 / 62.9 / 63.5 / 64.0 / 64.8 мин; mtime хука 09:29; на 10:21 нового события нет, а сессия занята аудитом. «Два хода после правки без строк» (§6 эпизода) при часовой каденции ничего не доказывает: под старым кодом эти два хода тоже дали бы ноль | accuracy_3 | -1 |
| 3 | Дискриминатор «ключ есть, значение пустое» держится на непроверяемом допущении | реплей 12 ключей реального payload: `""` / None / пробелы -> строки нет; `Task Tool`, `General Purpose`, `internal_subagent`, `2`, нет ключа -> `unroutable` на каждое событие. Журнал хранит `payload_keys` без значений (`traces/2026-09-08` строки 1-10), транскрипта по этим `agent_id` нет, проверить значение по истории нельзя | risk_24 | -1 |
| 4 | Новый guard сшивки и поле `note` не покрыты тестом | мутация `prev.get("agent") in TIER` -> `prev.get("agent")` на копии репозитория: `hook tests: 142/142 passed, 0 failed`; мутация со снятым `note` - тоже 142/142. Остальные пять мутаций дают от 1 до 20 FAIL | actionability_9 | -0.5 |
| 5 | Контроль §4 не вычислим как записан | `ls ~/.claude/projects/*/*/subagents/agent-*.jsonl \| wc -l` = 12, по meta-файлам 6 из 12 не-ростер (claude-code-guide 4, Explore 2); в `traces/2026-09-*` два `session_id` (bbcbbeee 78 subagent-записей, a0f0640f 22), а директория транскриптов есть только для bbcbbeee. Числитель и знаменатель считают разные популяции, порога нет | actionability_9 | -1 |
| 6 | `unroutable_stops` смешивает историю с новым сигналом и считает только stop | `trace-summary.py 2026-09` -> «Не распознано хуком P14: 73» при `grep -c unroutable traces/2026-09-*/agents.jsonl` = 0/0/0; на фикстуре из двух строк `unroutable` (start и stop) счётчик показывает 1 (`trace-summary.py:109`) | actionability_9 | -0.5 |
| 7 | Числа в шапке хука и в §1 не привязаны к состоянию | `subagent-trace.sh:9-10` («73 из 277», «одна строка на каждый ход», «outcome unknown») против `git show origin/main:traces/2026-09-0{6,7,8}/agents.jsonl` -> 147+120+9 = 276, unknown 72, outcome 68 unknown / 3 error / 1 success, каденция 08.09 почасовая. Там же §1 строка 18: «subagent_start 12, subagent_stop 83» против 11 / 81 на `origin/main` | accuracy_1 | -0.5 |
| 8 | Живой прогон, предъявленный как доказательство, записал ФЕНИКСУ чужую оценку | `traces/2026-09-08` строка 19: `feniks_score` 9.9 при фактическом weighted 7.7 (строка 18, `event: audit`). Механика воспроизведена: сообщение с `weighted_total 7.7` без двоеточия плюс цитата `weighted_total: 9.9` в evidence -> 9.9; с двоеточием у собственной оценки -> 7.7. Блок извлечения этим PR не менялся, но ни один новый контроль его не ловит: `trace-summary.py 2026-09` показывает mean 7.23 и max 9.9 | risk_21 | -0.5 |

Проверено и НЕ подтвердилось (в gaps не пошло):
- Потеря наблюдаемости Council через `general-purpose`: `.claude/skills/council/SKILL.md:68-76` - этот путь только Cowork, где проектных хуков нет.
- Расхождение whitelist с реальным именем плагина: `.claude-plugin/plugin.json` -> `gengroup-roster` 3.0.3, `marketplace.json` -> `gengroup-roster`. Совпадает, `gengroup-roster:feniks` под плагином трассируется.
- Регистрация хука: `SubagentStart` и `SubagentStop` есть и в `.claude/settings.json:110,121`, и в `.claude/hooks/hooks.json:55,66`, matcher пустой.
- «8 коммитов chore(traces) за ночь»: `git log --grep="chore(traces)"` -> с 00:53 до 08:11 08.09 ровно 8; всего с 06.09 их 54, то есть масштаб шума автор скорее занизил.
- Цифры §1: 276 = 147+120+9, unknown 72 (26%), `msg_chars` 4-24105, outcome 68/3/1 - воспроизведены полностью. Gaps 4 и 5 итерации 1 закрыты.
- `P14_TRACE_NONROSTER` в живых документах: `schemas/agent-trace.json`, `roster-protocol/SKILL.md`, шапка хука, тесты. Gap 7 итерации 1 закрыт.
- Контекст для CC-19, вне этого диффа: на `origin/main` за сентябрь события журнала - `subagent_start` 11, `subagent_stop` 81, `audit` 6, `gate` 178. Правка убирает 72 строки шума (26%), но 64% объёма журнала даёт другой хук (`feniks-bash-scope` пишет строку на каждый блок), и четыре такие строки за 08.09 - ложные срабатывания на командах аудита.

## 5 stress-test вопросов

**Q1 Доказательства.** Открыты все опорные цифры: 142/142 подтверждено прогоном, 276/72/4-24105/68-3-1 подтверждены
`git show` по `origin/main` 7931b61, 8 ночных коммитов подтверждены `git log`, живой прогон подтверждён независимо от
эпизода (сопоставление ctime транскриптов субагентов с журналом). Не подтверждено одно: что внутреннее событие
харнесса теперь не пишет строку. Это не «автор не приложил», это «событие ещё не наступило».

**Q2 Downside.** Если допущение о пустом значении неверно, правка не убирает шум, а переименовывает его: строка
`unroutable` на каждое событие, грязное дерево, коммиты `chore(traces)` продолжаются, плюс счётчик `unroutable_stops`
растёт и обесценивается. Стоимость обнаружения низкая (видно в первый же час), стоимость отката - один `git revert`.
Второй downside, уже воспроизведённый: чужой агент с именем `feniks@<что-угодно>` получает tier 0 и оценку в метрику CC-19.

**Q3 Ресурсы.** Bash и python3 из стандартной среды, тест 11 секунд, откат тривиален. Ресурсного риска нет.
Дефицит - не в ресурсах, а в наблюдении: правка живёт в рабочем дереве с 09:29, первое решающее событие приходит
без участия человека, но никто не назначен смотреть на него в ближайший час (rework п.7 итерации 1 отложен
«после мержа», хотя код работает уже сейчас).

**Q4 Что забыто.** Забыты: ось суффикса (gap 1), значение `agent_type` как непроверенное допущение (gap 3),
покрытие нового guard тестом (gap 4), сопоставимость сторон контроля (gap 5), разделение истории и нового сигнала
в счётчике (gap 6). Не забыто и проверено: whitelist соответствует имени плагина, регистрация хука в обоих местах,
запрет `feniks_score` не-ФЕНИКСУ (кейс t-m2 плюс мутация M3 итерации 1), схема допускает `unroutable`,
вердикт не приписывается нераспознанному (мутация M8).

**Q5 Инвестор-тест.** Первый вопрос: «вы починили шум - покажите час без шума». Ответа на 30 секунд нет: последнее
внутреннее событие было за 13 минут до правки, следующее приходит после возврата этого аудита. Всё остальное
показывается за минуту (142/142, 22 вектора имени, два живых субагента с противоположным результатом).

## Scores

| Критерий | Вес | Балл | Чем держится |
|---|---|---|---|
| ACCURACY | 25% | 8.0 | Все цифры эпизода воспроизведены до единицы, схема и документы согласованы; минус за шапку хука (73/277, «каждый ход», «outcome unknown») и за категоричность формулировки в трёх живых документах при недоказанном допущении |
| ACTIONABILITY | 25% | 8.0 | Владелец, дата сверки, откат, тест как условие мержа; минус за контроль, который не вычислим в записанном виде, и за счётчик, смешивающий историю с сигналом |
| INSIGHT | 20% | 7.0 | Признак «ключ есть, значение пустое» и канал `unroutable` вместо тишины - неочевидный ход; второй порядок самого `unroutable` не разобран, cross-domain нет |
| BRAND FIT | 15% | 10.0 | Em dash 0 во всех восьми файлах, Anti-Slop чисто, документация легла в правильные слои памяти (схема, skill, конституция), формат эпизода по Protocol 10 |
| RISK AWARENESS | 15% | 7.0 | Downside назван, откат описан, P9-триггеров нет; минус за незакрытую ось суффикса, за молчание о рисках нового пути и за отсутствие сценария «хук падает молча» (`2>/dev/null \|\| true` глотает любую ошибку) |

**weighted_total = 0.25×8.0 + 0.25×8.0 + 0.20×7.0 + 0.15×10.0 + 0.15×7.0 = 7.95** (пересчёт вручную сходится, дельта 0.00)

## rework_tz (для итерации 3, мерж не блокирует)

1. Зафиксировать первое реальное внутреннее событие после правки (первое окно - ближайший час после возврата этого
   аудита): строки нет или строка `unroutable` с `note`. Если `unroutable` - отсекать по признаку «ключ `agent_type`
   есть и значение не даёт имени ростера» и повторить наблюдение.
2. Whitelist на суффикс: принимать `@synced` и ` (gengroup-roster)`, остальное считать чужим.
3. Кейсы в `scope-cases.tsv`: guard сшивки (start не-ростера под `P14_TRACE_NONROSTER=1` плюс stop с тем же
   `agent_id` и пустым ключом, ожидание 0) и проверка `note`.
4. Контроль §4: транскрипты фильтровать по `agentType` из meta и по `session_id`, сравнивать с `subagent_start`
   того же `session_id`; порог задать числом после недели; записать, что транскрипты чужих сессий на диске отсутствуют.
5. `unroutable_stops` разделить на `unroutable` (новое) и `unknown` (история до 2026-09-08), считать оба события.
6. Шапку хука и §1 привести к проверенным числам: 72 из 276 на `origin/main` 7931b61, `subagent_start` 11 и
   `subagent_stop` 81, каденция 08.09 почасовая, outcome 68 unknown / 3 error / 1 success.
7. Отдельной задачей: извлечение `feniks_score` привязать к строке вердикта, а не к первому совпадению по всему
   сообщению (иначе цитата чужой оценки в отчёте становится оценкой отчёта).

## Ограничения аудита

- Правка не закоммичена, аудит идёт по рабочему дереву ветки `claude/spartak-roster-phoenix-skills-hgzf45`.
- Все пробы выполнены на фикстурах в `/tmp/**` и на копии `.claude/hooks` + `schemas` в `/tmp/**`; продуктовые файлы не изменялись.
- Записано только: этот отчёт, JSON рядом, строка `event: audit` в `traces/2026-09-08/agents.jsonl`.
- Значение ключа `agent_type` во внутренних событиях харнесса недоступно ни в журнале, ни в транскриптах: вывод об
  этом допущении сделан по реплею формы payload, а не по самому событию.
- Наблюдение за живым журналом велось до 10:21 UTC; главная сессия в это время была занята этим аудитом.
