# Хендофф сессии 2026-09-22 (ФИНАЛ) - продолжение в новой сессии через git

## START HERE (новая сессия, первое сообщение)
> «Прочитай `git show origin/claude/traces-flush-20260920:knowledge/episodes/2026-09/session-handoff-20260922.md` и методичку `...:knowledge/episodes/2026-09/dialog-v2-methodology-20260922.md`. Продолжаем дашборды GENGROUP. Гейт ФЕНИКСА по Диалоги v2 закрыт (go 8.4) и задеплоено - переделывать не надо. Жду мою задачу; если задачи нет - подтверди, что видишь все дашборды (инвентарь ниже) и назови остаточный бэклог.»

Разговор прошлой сессии в новую НЕ переносится - этот файл его замещает. Весь код в git (ветки ниже). Переехали ради экономии токенов (очень длинный контекст). Новая сессия = чистый контекст = дешевле.

## СТАТУС: всё закрыто, активных незавершённых задач НЕТ
Диалоги v2 прошли ФЕНИКС (go 8.4) и задеплоены (deploy-pages #2480, /dialog/ live). Всё остальное тоже в проде. Новая сессия начинает с чистого листа: либо новая задача Ивана, либо взять пункт из бэклога ниже. НЕ нужно: перезапускать ФЕНИКС, передеплаивать Диалоги, трогать РОП-дашборд.

## Ветки и последние коммиты (всё запушено)
- `dialog-export-v1` @ **cecc4710** - дашборд «Диалоги» (главная рабочая ветка). Содержит РАУНД 2 доработок по аудиту ФЕНИКСА.
- `rop-dashboard-v1` @ 3fb51465 - РОП-дашборд (меню-якорь по разделам). В проде.
- `claude/traces-flush-20260920` - только трейсы наблюдаемости (не продукт; стоп-хук требует их коммитить каждый ход).
- Деплой: workflow `deploy-pages.yml` (ref main). /dialog/ пересобирается build-dialog.ts (фетчит dialog-export-v1). /rop/ копирует готовый public/rop-command.html.

## ИНВЕНТАРЬ ДАШБОРДОВ (все, база https://oliverjone01-dev.github.io/t1)
Каждый собирается в deploy-pages.yml из СВОЕЙ ветки-источника (правим через worktree этой ветки, деплой копирует/пересобирает в /site/<путь>).

| Дашборд | URL | Ветка-источник | Как собирается / снимок |
|---|---|---|---|
| **Хаб (общая страница)** | `/` (+ логотип ГГ ведёт сюда) | main | `analytics-mvp/public/dashboards/index.html`; пароль HUB_PASS. Меню всех дашбордов. |
| **РОП ГГ** | `/rop/` (+ `/rop2/`) | rop-dashboard-v1 | build-rop.ts -> public/rop-command.html; снимок b24-snapshots.yml (cron 02:20). Меню-якорь по разделам. |
| **РОП ГМ** (Metal-GM) | `/rop-gm/` | rop-gm-dashboard-v1 | build-rop-gm.ts на деплое; снимок b24-gm-snapshots -> rop-gm.json. |
| **Менеджеры (личные)** | `/rop-<фамилия>/` | manager-lakomova + rop-dashboard-v1 | build-managers-all.mjs (MIN_DEALS=5). Лакомова/Лысанова/Шура-Бура/Платонова/Зазноба/Лобова и др. У каждого свой лого/периоды (личные - в этой сессии НЕ трогали по просьбе Ивана). |
| **Экономика** | `/economics/` (+ `/econ-control/`) | economics-dashboard-v1 | build-econ-control.mjs -> public/econ-control.html (это боевой /economics/). layers.html = старый economics-command. Лого+периоды+цветная дата обновления сделаны. |
| **Диалог (хронология коммуникаций)** | `/dialog/` | dialog-export-v1 | build-dialog.ts на деплое; снимок b24-dialog-cron -> dialog.json (~93МБ). |
| **ИИ-анализатор в Диалогах** | внутри `/dialog/` | dialog-export-v1 | score-dialog.ts (модель: prob/градусник/факторы/очередь/калибровка) + ai-review.ts (API-разбор) + ручной оверлей `dialog/data/ai-tags-manual.json`. Колонка «Разметка», очередь дел, температура. **Тут идут изменения v2 (ждут ФЕНИКС+деплой).** |
| **Диалог-эксперименты (песочница Ивана)** | `/dialog-experiments/` | dialog-experiments | build-svod2.ts (СВОД) + dialog.html; отрезана от боевого 2026-08-28, /dialog/ не трогает. |
| **Производство (PROD)** | `/prod/` (+ `/prod2/`) | prod-dashboard-v1 | prod-command.html. |
| **Офис-менеджеры** | `/office/` | office-dashboard-v1 | office-command.html (лид-интейк, не оценивается регламентом продавца). |
| **Маркетплейсы (WB/ЯМ + Katya OZON)** | `/market/` | main (data-ym) | build-site.ts + build-katya.ts (PLATFORM=ym). Katya OZON - свод по OZON. |
| **Яндекс.Директ** | `/direct/` (превью `/direct-preview/`) | main (yandex-direct/) | dashboard/public. Владелец скилла - timur. |
| **Отчёты (Telegram-бот)** | чат `-1004401671894` | main | `rop-tg-bot.yml`: режимы plan(🌅)/itogi(🌙)/report(md)/probe/test. Данные из rop.json. «Срез дня» отключён. |
| Прочее в хабе | `/seo/` `/plan/`(+v2) `/markplan/` `/integra/`(GENTERO) `/ozon-research/` `/phoenix/` `/op-gm/` `/messages/` `/academy/`(разборы Кости, запечатано) `/kp-gm/` | свои ветки | см. соответствующие шаги deploy-pages.yml |

Отчёты «все»: (1) Telegram-дайджесты plan/itog (rop-tg-bot); (2) режим report - готовые md-отчёты в чат; (3) Kostya-AI ежедневный разбор менеджеров (skill kostya-ai -> /academy/); (4) аналитика OZON/МП (analytics-mvp/, Katya). Все живут поверх снимков Bitrix/OZON.

## ГЕЙТ ФЕНИКСА ЗАКРЫТ (go 8.4/10) - «Диалоги v2» задеплоено
- Изменения «Диалоги v2» (калибровка весов + новые факторы + очередь дел) - смена методики оценки менеджеров -> прошли ФЕНИКС Step 12.5 (CLAUDE.md §4/§15).
- Раунд 1: return 7.5 (client_chase давал ложные обвинения - L1 FAIL). Раунд 2 (cecc4710): дефекты исправлены -> **ФЕНИКС go 8.4/10**, блокеров нет, деплой разрешён и выполнен (deploy-pages, ref main).
- Отчёты ФЕНИКСА (эфемерно, scratchpad прошлой сессии): feniks-report.json (iter1), feniks-report2.json (iter2 go). Суть - в этой методичке.

### Остаточные gaps ФЕНИКСА (НЕ блокеры, СЛЕДУЮЩАЯ ИТЕРАЦИЯ - в dialog-export-v1)
1. **ball не загейчен на !hotSlow** (score-dialog.ts, строка с `push("ball"...)`). hot_slow+ball стекаются на ~320 сделках, одна незакрытая горячая реплика штрафует дважды (0.6×0.7). Не ложное обвинение, но магнитуда ~+40%. Фикс: добавить `&& !hotSlow` к ball ИЛИ брать min (как сделано для slow_resp).
2. **hot_slow вес 0.6 не откалиброван**, теперь на ~461 сделке. Следить по приёмке item2 (владелец Иван, +2 нед по calib-log false-positive rate).
3. Мелочь: RE_HOT содержит широкое «есть ли» (~4.7% срабатываний только на нём); framing непоследователен - hot_slow тон bad, а client_chase warn+«повод спросить» при схожей мягкости. Выровнять.
4. Через 2-4 недели: чистая калибровка из calib-log (вариант B) без конфаундинга.

## Что уже В ПРОДЕ (задеплоено в прошлых сессиях)
- Диалоги: фикс «Разметка» (механический разбор коммуникаций не прячется, порепличный fallback ИИ/regex). deploy #2467.
- Диалоги: ручной ИИ-разбор Лысановой - 79 тегов/29 сделок, оверлей `dialog/data/ai-tags-manual.json`, подмешивается build-dialog.ts при каждой сборке (переживает ночные снимки).
- РОП: меню-якорь по 6 разделам в шапке. deploy #2453.
- Бот `rop-tg-bot.yml`: план и итог дня в основной чат `-1004401671894` (msg 538/537). «СРЕЗ ДНЯ» отключён.

## Диалоги v2 - что в ветке cecc4710, НЕ в проде до деплоя+ФЕНИКС go
Файлы: `analytics-mvp/src/scripts/b24/score-dialog.ts`, `.../dialog/dialog.template.html`, `.../dialog/data/dialog.json` (пересчитан), `.../dialog/data/calib-log.ndjson` (лог для варианта B).
1. Калибровка весов на исходах (осторожная, A): 3 устойчивых сигнала с предохранителями. slow_resp 0.8->0.66, refuse 0.5->0.44, ready 1.3->1.39. Остальные ~15 весов заморожены (наивная калибровка конфаундится терминальным срезом - см. методичку).
2. Лог состояний (B): calib-log.ndjson -> чистая калибровка без конфаундинга через 2-4 недели.
3. Новые факторы: client_chase (клиент напоминает сам, порог 4 раб.ч, tone warn) + hot_slow/hot_fast (латентность на горячей реплике, RE_HOT: наличие/сроки/склад/готовность/оплата).
4. Очередь дел РОПа - верхний экран renderTree, сортировка по prio (деньги×шанс×срочность) + плашка калибровки (def->mult, n, conf).

## Бэклог (обсуждено, не начато)
- Полный ИИ-разбор по API (msgTags на все реплики всех сделок): ~$4-7 Haiku-batch / $8-14 Sonnet-batch; ai-review.ts донастроить на выдачу msgTags. Лимит бюджета $15, потрачено ~$2.2. Ждёт решения Ивана.
- Через 2-4 недели: снять чистые веса из calib-log (вариант B) -> повторная калибровка без конфаундинга.
- Идеи в градусник (не начато, только для Диалогов - РОП-дашборд НЕ трогать): сентимент клиента до отказа; квалификация BANT как нагрев; тип клиента (комплектатор/дизайнер) в приоритет; пост-оплатный SLA.

## Правила/гейты (CLAUDE.md) и гоча
- Смена методики оценки менеджеров -> ФЕНИКС Step 12.5 + HITL Иван. Kill: veto (<6) -> стоп, эскалация.
- Скоуп: только дашборд Диалоги. РОП-дашборд НЕ трогать (решение Ивана).
- Стоп-хук: коммитить traces/YYYY-MM-DD/agents.jsonl в ветку claude/traces-flush-20260920 каждый ход.
- Данные-ветки правим через git worktree (origin/<branch> --detach), собираем локально (npx tsx build-*.ts / score-dialog.ts с ROP_JSON=/tmp/rop.json), коммитим 2-3 целевых файла, public/*.html для /dialog/ НЕ коммитим (деплой пересобирает), для /rop/ - коммитим. Worktree убирать после (git worktree remove --force). Force-push блокируется классификатором - использовать новые ветки/PR.
- egress-прокси блокирует oliverjone01-dev.github.io (живую страницу curl-ить нельзя; проверять через собранный html + Actions-логи).
- dialog.json ~93МБ (git предупреждает Large files - это норма для этой ветки).
- MCP github dispatch атрибутируется как oliverjone01-dev (это ок).
- Эфемерно (пропадёт при смене сессии, но всё важное уже в git): scratchpad прошлой сессии (скрипты, копия dialog.json). Методичка сохранена в репозиторий (dialog-v2-methodology-20260922.md).
- **PR #70 «РОП дашборд v1 (WIP)» - это шум, не задача.** Его head = ветка данных rop-dashboard-v1, поэтому каждый ночной снимок/пуш капает туда коммит и будит сессию событиями CI. Вечный draft (base main, 767 коммитов, конфликты), НЕ на мерж - это живая ветка данных РОП-дашборда. События check_suite по нему игнорировать, если нет реального failure по нашей задаче. Аналогично может быть по dialog-export-v1.
