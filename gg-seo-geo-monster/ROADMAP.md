# ROADMAP - GG-SEO/GEO Монстр

Путь от скелета до боевого сайта на отдельном URL. Даты - целевые, не обещания.
Каждый этап закрывается замером, а не ощущением «готово».

## Этап 0 - Скелет (сделано)

- [x] Charter проекта (`README.md`)
- [x] Хаб-лендинг с витриной модулей и статусом (`public/index.html`)
- [x] Структура папок и sample-данные (`data/*.sample.json`)

## Этап 1 - Наполнение хаба живыми данными (цель: 2 недели)

- [ ] `data/semcore/` - частотность запросов из Yandex Wordstat (скрипт
      `scripts/wordstat.mjs` + workflow `wordstat-semcore.yml`, сиды в
      `data/seed-phrases.json`). Готово к запуску, ждёт секреты Яндекса (см.
      `docs/WORDSTAT_API.md`). Собирает семантическое ядро на проекты.
- [ ] `data/clusters.json` - карта кластеров pillar/satellite: частота из Wordstat
      (`[ДАННЫЕ]` с датой) + интент из единой Google-таблицы контент-плана
- [ ] `data/citations.json` - baseline замер AI-видимости по банку из 50+ промптов
      (6 типов), метрики Prompt Win Rate / Share of Model / Coverage / Answer Share
      (определения - `knowledge/geo-intel-intake.md`). Владелец: semyon + data, каждая цифра с датой
- [ ] Хаб читает JSON, а не хардкод в HTML
- [ ] feniks Step 12.5 на baseline-замер (нельзя показывать непроверенные цифры)
- [ ] Разведка рунета `knowledge/geo-intel-runet-2026.html` - оформить страницей хаба
      (раздел «Разведка GEO») и источником определений KPI для AI Citation Tracker

### Доступы Google, которые нужны от Ивана (Этап 1)

Минимальный вариант, по образцу уже работающих в репо импортов (auth не нужен,
секреты не заводим):

1. **Единая таблица контент-плана** - открыть «Все, у кого есть ссылка → Читатель»
   и прислать ссылку. GitHub Action тянет CSV-экспорт вкладки, без OAuth.
   Структура колонок - в `data/content-plan.schema.md`.
2. **Google Docs со статьями** - на приёмку Софии доступ хабу не обязателен
   (хаб показывает статус, не тело статьи). Если позже понадобится тянуть текст -
   решим отдельно (публикация Doc «по ссылке» либо доступ через Google Drive MCP).

Расширенный вариант (если захотим запись/чтение приватных файлов через Google
Drive MCP-коннектор): дать доступ подключённому Google-аккаунту к конкретной
папке проекта. Пока не требуется - начинаем с CSV-экспорта.

## Этап 2 - Content Forge UI (цель: +3 недели)

- [ ] Форма: кластер ключей → вызов промпта (`../SEO_PIPELINE_content_forge_prompt.md`)
- [ ] Level 1 авто-checks (Entity Definition, ≥3 числа на H2, Anti-Slop, em dash)
- [ ] Предпросмотр 6 блоков + экспорт Schema.org JSON-LD

## Этап 3 - Публикация на GitHub Pages /seo/ (цель: после этапов 1-2)

Хостинг уже прописан в `deploy-pages.yml` (подпапка `/seo/`, push в `main`).
Остаётся довести содержимое и пройти гейт.

- [ ] feniks финальный аудит перед первым публичным деплоем
- [ ] Мердж в `main` → авто-публикация в `/seo/`
- [ ] Проверка Schema.org через validator.schema.org на живой странице
- [ ] Отдельный домен - позже, отдельным решением (смена базового URL, не переписывание)

## Kill Criteria (авто-stop, Protocol 6)

- feniks score < 5.0/10 на любом артефакте хаба
- Показ цифры цитируемости/позиции без источника и даты (Protocol 9 hard rule)
- Прямая команда Ивана «СТОП»

## Решения Ивана (07.07.2026)

1. **Домен:** пока хостим на GitHub Pages Ивана (`/seo/`), без отдельного домена.
   Отдельный домен - позже, отдельным решением.
2. **Охват:** все 5 брендов холдинга, приоритет GENGLASS.
3. **Приёмка:** контент-менеджер - София. Статьи в Google Docs, единый
   контент-план в Google-таблице.

## Осталось получить от Ивана

- Ссылку на единую Google-таблицу контент-плана, открытую «по ссылке - Читатель»
  (см. «Доступы Google» в Этапе 1).
- Секреты Яндекса для сбора частот (один набор, см. `docs/WORDSTAT_API.md`):
  вариант A (AI Studio) - `YANDEX_SEARCH_API_KEY` + `YANDEX_FOLDER_ID`;
  вариант B (standalone) - `YANDEX_OAUTH_TOKEN`. Плюс подтвердить, какой доступ есть.
