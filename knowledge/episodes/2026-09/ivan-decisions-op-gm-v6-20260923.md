# Решения Ивана по op-gm v6 (Контур DS 1.5), 23.09.2026

Контекст: Step 12.5, ФЕНИКС, итерации 1-2 (return 7.70 и 8.30). Эскалации из отчёта итерации 2.

1. **Пароль.** Сменён (решение 23.09). Свойства пароля в репозиторий, эпизоды и трейсы не пишутся.
   - Описание устройства пароля было в застейдженном README и в коммит не попало: строка удалена до коммита.
   - Этот риск Иван снимает: «снять риск, пропускаем и идём дальше».
   - Понижающий override по нему в следующих итерациях не применяется.
   - Остаточный риск по истории git (старые шифроблоки v1-v5 и подсказка в прошлой версии эпизода v4) принят раньше.
2. **Google Fonts.** Шрифт Golos Text с Google Fonts, как предписывает Контур DS, оставить.
   - Допустима замена на максимально схожий шрифт, если внешний запрос станет проблемой.
   - Сейчас оставляем как есть.
3. **Дефекты пакета Контур DS** - отдельной задачей, после того как op-gm v6 пройдёт Step 12.5, смержится и выйдет в прод:
   - сторож ловит 5 из 16 нарушений (полоса через легаси-алиас, полоса-span и др.);
   - глобальный ключ ks-prefs без пространства имён;
   - пункты палитры команд 34 px на касании, шапка шаблона на телефоне (obzor.html);
   - числовой форматтер оси портит подписи дней на тепловой карте.
