# Разовый экспорт контактов Wilstream (колл-центр, обзвон мастерских для GLASS-MEMORY) в .xlsx.
# Запускается пушем этого файла или скрипта в ветку claude/dashboard-path-git-xhd1vt.
# Данные из Bitrix24 (секрет B24_WEBHOOK_URL), только чтение.
name: Экспорт контактов Wilstream

on:
  workflow_dispatch: {}
  push:
    branches: [claude/dashboard-path-git-xhd1vt]
    paths:
      - .github/workflows/export-wilstream.yml
      - analytics-mvp/src/scripts/b24/export-wilstream.mjs

permissions:
  contents: write

concurrency:
  group: export-wilstream
  cancel-in-progress: false

env:
  B24_WEBHOOK_URL: ${{ secrets.B24_WEBHOOK_URL }}

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Проверка секрета
        run: |
          echo "::add-mask::$B24_WEBHOOK_URL"
          if [ -z "${B24_WEBHOOK_URL:-}" ]; then
            echo "::error::Секрет B24_WEBHOOK_URL не задан."; exit 1
          fi

      - name: Экспорт из Bitrix -> xlsx
        working-directory: analytics-mvp
        run: node src/scripts/b24/export-wilstream.mjs

      - name: Артефакт
        uses: actions/upload-artifact@v4
        with:
          name: wilstream-contacts
          path: |
            analytics-mvp/exports/wilstream-contacts.xlsx
            analytics-mvp/exports/wilstream-log.txt
          if-no-files-found: error

      - name: Коммит файла в ветку
        working-directory: analytics-mvp
        run: |
          set -e
          git config user.name "gengroup-export-bot"
          git config user.email "export-bot@gengroup.local"
          git add -f exports/wilstream-contacts.xlsx exports/wilstream-log.txt
          git commit -m "export: контакты Wilstream" || { echo "нет изменений"; exit 0; }
          B="${GITHUB_REF_NAME}"
          for i in 1 2 3 4; do
            git push origin HEAD:"$B" && break || {
              git fetch origin "$B" && git rebase origin/"$B" || true; sleep $((i*2)); }
          done
