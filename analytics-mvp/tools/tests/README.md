# tools/tests - исходники вкладки «Тесты» (OZON)

A/B-тесты по рекламе OZON (ставка/соинвест). Вкладка построена: `src/scripts/build-tests.ts` →
`public/katya-tests.html` (`npm run tests:page`).

- `tests.json` - определения тестов (гипотезы, тест/контроль группы SKU, старт→замер, правила, метрики).
- `tests_campaigns.psv` - лог кампаний по SKU (показы, соинвест, ставка рекоменд→финальная, старт, контроль).
- `ads_campaign_history_2026-06-13.psv` - полный лог действий по рекламным кампаниям кабинета.
- `katya-tests-mock.html`, `gen_tests_mock.py` - первый макет вкладки, оставлен как история решения.
- `payout_base_check.py` - проверка «кто платит скидку»: к чему привязана выплата на единицу,
  к предельной цене или к витрине. Запуск: `python3 tools/tests/payout_base_check.py`.

## Эталон соинвеста

`data/payout_orders.ndjson` - выгрузка реестра «Начисления» с разбитой строкой «Баллы за скидки»,
311 заказов августа. По каждому заказу: цена продавца, сколько заплатил покупатель, сколько доплатил
Ozon, программы партнёров. Тождество `цена продавца = покупатель + баллы + партнёрские` сходится до
рубля на 310 строках из 311 (у `05063721-0463-1` в выгрузке `qty=1`, а суммы идут за две штуки).

Это единственный источник, по которому соинвест можно проверить: наш ряд `data/coinv_daily.ndjson`
считается из пары цен снимка и сам по себе не проверяем.

Калибровка: `npm run coinv:calib` → `data/coinv_calibration.json`. Скрипт подставляет фактические
цены по одной и тем называет виновную цену, а не просто показывает расхождение. Идёт ежемесячно в
`orders-backfill.yml` вместе со сбором начислений.

Состояние на 23.09 по августу, 273 сопоставленных заказа, 90 % оборота:

| | |
|---|---|
| соинвест по витрине | 49.2 % |
| соинвест по цене с картой Ozon | 54.3 % |
| факт | 57.4 % |
| срез по стабильной цене (23 заказа): карта против факта | 51.6 % против 52.1 % |

Единица счёта - заказ. Сравнивать медиану по каталогу с медианой по заказам нельзя: заказывают
чаще уценённое, по каталогу за август выходит 46 %, по заказанным товарам 57 %, и это разные
совокупности, а не расхождение метрики.

Правильная цена - та, что платит покупатель с картой Ozon. В Seller API это `marketing_price`
(`marketing_seller_price` учитывает только акции продавца), в BFF кабинета то же поле зовётся
`marketing_oa_price`. `connector.priceItem()` берёт первое, откатываясь на второе.

Остаточные 3 пункта на всей выборке - шум месячной свёртки, а не ошибка метрики: эталон не несёт
даты заказа, поэтому снимок артикула сворачивается в медиану за месяц. На артикулах, у которых
цена внутри августа почти не двигалась, расхождение падает до 0.5 пункта.

Чего не хватает: 26 артикулов из 146 в эталоне отсутствуют в снимке цен, это 10 % оборота.
Флаг `visibility` тут ни при чём, в коннекторе уже стоит `ALL`, и наш собственный
`prices_daily.ndjson` видит ровно те же 514 артикулов, что и снимок Ивана. При этом у 13 из 26
показы в поиске шли до 10-21.09, то есть они живые. Похоже на архив каталога, который под `ALL`
не отдаётся; подтвердить можно только запросом с ключами.

Полный контекст: `knowledge/episodes/2026-09/session-handoff-katya-ozon-20260922.md` и
`knowledge/episodes/2026-09/feniks-veto-tests-coinv-20260923.md`.
