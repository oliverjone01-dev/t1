# BACKLOG - GEO-monster

Отранжировано по ценности. Верхние пять - рекомендуемый порядок.

1. ~~Сверить эндпоинты keys.so~~ **СДЕЛАНО 10.07** (DECISIONS D13): групповой отчёт
   для фраз, concurents для конкурентов, dashboard подтверждён живым запросом.
   Остаток: сверить имена полей в строках ответа (pos/ws/url) на первом успешном
   прогоне группового отчёта - защитный pick() подстрахует, но глазами проверить.
2. **Счётчики Метрики + `YM_TOKEN`/`YD_TOKEN`.** Заполнить `metrika_counter` обоих
   проектов в `config/projects.json`, добавить секреты - Метрика и Директ оживут,
   включится фича «Директ платит, а органики нет» на живых данных.
3. **Цели Метрики (конверсии).** Выбрать goal_id по каждому проекту и добавить в
   harvest (сейчас `goals: []`, конверсии не выдумываем - D8).
4. **Проверить домен третьего конкурента glass-memory** («promsteklo» из мастер-промта
   помечен как примерный - D9) и вписать реальный в config + keyso-targets.
5. **AI Citation Tracker: связка с «Трекер ИИ» keys.so.** В кабинете есть встроенный
   трекер ИИ-выдачи - когда появится его эндпоинт в API, кормить M3 живыми данными
   вместо только ручного журнала.
6. Запись `projects.json` из админки напрямую через GitHub API с fine-grained PAT в
   localStorage (опция из мастер-промта; сейчас - экспорт файла).
7. Lighthouse-прогон в CI (сейчас перф обеспечен архитектурно: статика без бандла).
8. Автообнаружение новых конкурентов по пересечению выдачи ≥15% (harvest уже тянет
   топ-15 пересечений; добавить сравнение со списком в config и сигнал).
9. Wordstat-сборщик: подключить частотку к M2 как второй источник freq (скрипт и
   workflow готовы, ждут `YANDEX_OAUTH_TOKEN`).
10. Мобильный kanban: drag-and-drop вместо кнопок-стрелок.
11. Тюнинг Директ-буста на живых данных (ФЕНИКС-гейт): overlap >= 2 токенов может
    ложно метить «Директ платит» на общих парах слов; поднять порог или требовать
    совпадения кластера, в тег добавить число общих токенов.
12. Решение владельца по публичности данных (SETUP.md, раздел «ВАЖНО»): приватный
    репо или отказ от YD_TOKEN, если расход Директа нельзя публиковать.
13. bounce_rate/depth в harvest читаются позиционно из totals Метрики - заменить на
    чтение по имени метрики (хрупко при смене порядка).
