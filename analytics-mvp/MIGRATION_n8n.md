# Миграция с n8n на прямые OZON-клиенты в репозитории

Цель (Иван): убрать n8n (у n8n Cloud кончился лимит выполнений), перенести всю логику
в репозиторий, «без лишних колен». Гейт: премка ФЕНИКСА (Step 12.5).

## Статус

| Этап | Что | Статус |
|---|---|---|
| 1 (proof) | `pnl-sku` прямой OZON, сверка 1:1 | ✅ **верифицировано** в CI: 209 SKU, 0 расхождений |
| 2 | портировать skus/pnl/daily/ads | ✅ **код готов** (см. ниже), верификация — после секретов |
| 3 | снять живые fetch, чистить n8n-пайплайн | ⛔ за гейтом (нужны верифицированные продьюсеры + решение) |
| 4 | Гуру-чат | ⛔ решение Ивана (убрать / Worker / клиентский ключ) |

## Продьюсеры (всё на `connector/` + `util/retry.ts withRetry`, креды из env)

- `src/scripts/backfill.ts` — **daily** история (уже был, на `OzonSeller.skuDaily`, с DQ).
- `src/scripts/ozon/pnl-sku.ts` — `OzonSeller.transactions` → bySku. **1:1 проверено.**
- `src/scripts/ozon/pnl.ts` — канальный P&L.
- `src/scripts/ozon/skus.ts` — analytics+stocks+prices(v5) + ДРР по линиям (Performance).
- `src/scripts/ozon/ads.ts` — кампании/сливы/top_spend/by_line (Performance). Per-SKU отчёт — ленивый/кэш, не трогаем. **Пилотировать отдельно (FENIX).**
- `validate-snapshot.ts` — добавлен **дельта-гейт**: блок замены снимка при просадке ключевой метрики >40%.

## ФЕНИКС RETURN 5.6 — блокеры до исполнения

- **G1** — креды OZON НЕЛЬЗЯ через `workflow_dispatch input` (не маскируются, публичный репо). Вектор удалён.
- **G2** — репо **публичное**; история не просканирована на креды.
- При proof-прогоне Seller Api-Key засветился в публичных метаданных Actions → **скомпрометирован**.

## ⛔ Нужно от Ивана (Этап 0, до любого исполнения)

1. **Ротировать `OZON Seller Api-Key`** (засвечен публично). Горит.
2. **Сделать репозиторий приватным** (или осознанно подтвердить публичность).
3. Положить в **GitHub Secrets**: `OZON_SELLER_CLIENT_ID`, `OZON_SELLER_API_KEY`, `OZON_PERF_CLIENT_ID`, `OZON_PERF_SECRET`. (`sync.yml` их уже ждёт; агент сам секреты выставить не может.)

## Живые fetch в браузере (инвентаризация для Этапа 3, G6)

6 точек в `build-katya.ts`:
- Маркетинг: `ADS_URL` (кампании за период), `PSK_URL` (комиссия), per-SKU отчёт (lazy, report-mode).
- Деньги: `PNL_URL`, `PSK_URL`.
- Командный центр: `CC_ADS_URL` (общий ДРР).
- Гуру: `GURU_URL` (ИИ-чат).

Живой запрос уточняет снимок под ТОЧНЫЙ период. Снимок есть только для фиксированных 7/30/90.
**Перед снятием live-fetch:** либо запечь все выбираемые периоды, либо урезать выбор до 7/30/90.
Иначе кастомный период покажет данные не за то окно (тихо). Гуру — отдельное решение (Protocol 6).

## Что делаю после действий Ивана (1-3)

1. Верифицирую pnl/pnl-sku/skus в CI через Secrets (без input).
2. Пилотирую и сверяю `ads` отдельным прогоном (как pnl-sku).
3. Навешиваю дельта-гейт на замену всех снимков в cron.
4. Переключаю `update-snapshots.yml` на прямые скрипты, включаю `sync.yml`, выпиливаю вызовы n8n.
5. Инвентаризирую/баку периоды, снимаю live-fetch (или урезаю периоды) по списку выше.
6. Гуру + дата отключения n8n — на повторный гейт ФЕНИКСА.
