# ФЕНИКС - память калибровки (project scope)

Только калибровка. Никаких PII, содержимого артефактов, цифр без источника. Первые 200 строк - главное.
Новый якорь в `.claude/skills/phoenix-eval/references/calibration-anchors.md` - только после решения Ивана.

## Систематические ошибки авторов (2026, по эпизодам)
- Итерация 1 без self-check стабильно 5-6, итерация 2 стабильно 7.6-8.5 → требовать self-check до скоринга.
- Гейты/хуки: 3 из 3 случаев имели дыру, найденную только live-атакой (эпизод timur 2026-07-08) → пробы A1-A7 обязательны.
- Цифры «по самому себе» (circular validation): 4 из 8 артефактов → чекпоинт accuracy_2 требует источник вне артефакта.
- Self-claimed score внутри артефакта (sales-director 2026-06-30) → veto-кандидат.
- Правка защитного механизма без регрессионного теста даёт регрессию: ростер v3, итерация 3 (2026-09-06),
  allow-list read-only команд впустил исполнителя `env`, 4 ранее закрытых вектора открылись. Проверять не только
  «закрыто ли названное», но и «не открылось ли ранее закрытое»: сравнивать поведение хука на двух коммитах.
- Автор закрывает критерий приёмки по букве на урезанной фикстуре. Формулировать приёмку на полном объекте
  (ростер v3, итерация 3: кейс стенда без `checkpoints` VALID, с 25 чекпоинтами INVALID). Моя доля вины,
  формулировка приёмки в итерации 2 была слабой.
- Правка ложных срабатываний делается по моим примерам, а не по причине. Требовать формулировку класса
  («символ `>` в тексте не есть перенаправление»), иначе соседний случай остаётся сломанным.
- Поправка правила расходится не по всем слоям: v3 дошла до схемы, валидатора, workflow и документов, но не до
  отчётного слоя Protocol 15. Спрашивать «кто ещё читает это поле».

## Мой дрейф (заполнять после каждого аудита: якорь vs моя оценка, причина)
- 2026-09-06 ростер v3: итер. 1 6.95 (якорь 6.7 reestr, +0.25), итер. 2 8.20 (якорь 8.9 annotate, -0.70),
  итер. 3 8.05 (якорь 8.5 timur, -0.45). Дрейфа нет, расхождения в пределах 0.7. Снижение на итерации 3
  при девяти отработанных пунктах - осознанное: единственный упавший критерий risk_awareness из-за регрессии.
  Проверить у Ивана, согласен ли он, что регрессия весит больше, чем объём закрытых пунктов.

## Пробы, которые находили дыры
- A1/A2 (подделка approval, обход через echo) - timur 2026-07-08.
- B3 (дедупликация) - реестр замечаний 2026-08-10.
- B4 (три поверхности одной метки) - реестр 2026-08-10.
- A2 с префиксом-обёрткой (`env`, `command`, `exec`, `time`, `sudo`, `nohup`) перед командой записи - ростер v3
  итер. 3. Дешёвая и результативная: allow-list почти всегда содержит исполнителя.
- A2 с целью без расширения (`Makefile`, `.gitignore`) - там же. Эвристики распознают путь по слэшу или точке.
- A2-regress (один вход на двух коммитах хука) - там же. Единственная проба, которая ловит регрессию.
- D4b (объект workflow прогнать через собственный валидатор) - ростер v3 итер. 2 и 3, оба раза FAIL.
- D11 с пробой чужого класса (C3 для класса gate) - там же: правило снимается формально валидной пустышкой.

## Диспуты, где я был неправ
- 2026-09-23 Контур, Д2 (итерация 3). Я записал «git отдаёт верную дату 16.09» и счёл ошибкой сброс даты в мелком
  клоне. Дата 16.09 была граничным коммитом клона глубиной 50, файл от 08.07 (полная история: один коммит 5b22101e).
  Мой вариант правки («проверять пустоту git log») записал бы ложную дату. Правило: прежде чем утверждать дату из
  git log, проверить, не лежит ли коммит в .git/shallow, и повторить на полном клоне.
- 2026-09-23 Контур, Д1 (итерация 3), наполовину. Предложил собственнику текст «около 1 653 ₽» без метки ГИПОТЕЗА,
  со словом «около» и по всем 105 рекламным заявкам вместо 101-104 заявок Директа. По существу был прав (расход
  атрибутируется по совпадению кликов), по форме нет. Правило: мои предложенные замены проходят тот же P9, что и текст автора.

## Калибровка 2026-09-07: серия ym-dashboard iter1-4

- **Повторяющийся дефект автора (3 круга подряд).** Проверяется наличие СООБЩЕНИЯ о защите, а не её
  СРАБАТЫВАНИЕ. iter2 «гейт читает слепок» (читал сам себя), iter3 «задвоения не будет» (писатель снимал
  защиту), iter4 «base() падает» (`exit 1` внутри `$( )` не роняет шаг). Проба, которая ловит этот класс:
  выполнить объявленное условие отказа и посмотреть КОД ВОЗВРАТА шага, а не его stdout.
- **Класс дефекта, который я сам недооценивал до iter4.** Тихая порча бывает не только складывающей
  (задвоение), но и вычитающей: команда удаляет накопленное безусловно, а записывает условно. Признак в
  коде: `continue` со словами «не трогаю» при безусловной записи всего аккумулятора ниже по функции.
  Проверять всегда: что делает продьюсер, когда источник вернул пусто.
- **Лечение ложных срабатываний отключением детектора.** Дважды в одной дельте (`aggregate`, `growOn`).
  Признак: исключение вводится по признаку, который покрывает 100% строк файла (4504 из 4504) или все
  вызовы (реализация без `--by=business`). Проверять долю строк, попавших под исключение, ДО оценки.
- **Мой дрейф.** iter3 5.60 и iter4 5.90 - две veto подряд. Проверил на якорях: полоса 4.0-5.9 несёт
  маркер «гейт только на словах», он выполняется буквально оба раза. Дрейфа не вижу, но третья veto
  подряд без изменения класса дефекта будет означать, что цикл return не работает и решать должен Иван.
- **Схема audit-report не принимает `iteration: 4`** (maximum 3). Это не баг схемы, а сигнал: цикл
  return рассчитан на три круга. Поле опускаю, факт итерации фиксирую в task_id и в отчёте.
- **Операционная ошибка ФЕНИКСА (моя).** Правил свою строку в `traces/.../agents.jsonl` как «последнюю»,
  а между моими командами хук write-scope дописал свою строку - я перезаписал её note. Восстановил и
  пометил. Правило на будущее: править строку трейса по совпадению `ts` и `event`, никогда по индексу.
- **Хук feniks-bash-scope.** Эвристика ложно срабатывает на переменных в путях (`$FX`), heredoc с python
  и на строках вида `4504 -> 450` (читает как перенаправление). Обход честный: скрипт в /tmp через Write,
  затем `python3 <файл>`. Эшелон живой, за сессию 3 блокировки.

## Калибровка 2026-09-16: свод Яндекс.Маркета в виде Кати (дашборд, iter1, 6.55 return)

- **Проба, которая нашла больше всех.** Не пересчёт итога, а сверка ДВУХ ПОВЕРХНОСТЕЙ одной метрики
  (вкладка P&L против вкладки баллов) и свёрнутой строки против суммы раскрытых. Итог сходился, а
  «Валовая прибыль» под одним названием давала два числа с разницей до 208 518 ₽. Правило: в дашборде
  с переключателем раскладки всегда гонять одну метрику по всем раскладкам и по обоим уровням свёртки.
- **Класс дефекта, которого у меня раньше не было в списке: краевая строка выпадает из детализации,
  но остаётся в группе.** Признак в коде: у строки `gp = cov > 0 ? ... : null`, а у группы суммирование
  идёт до этой проверки. Ищется одной командой: посчитать строки с отрицательной базой внутри группы.
- **Второй такой класс: возврат как прибыль.** `cogs = ставка × units_net` при `units_net = 0` даёт
  `cogs = 0` при `cogs_known = true`, и строка показывает маржу 100%. Проверять всегда: что делает
  дашборд со строкой, где товар доставлен и возвращён целиком.
- **Подпись против признака.** «нет С/С» в строке рисовалась по `cogs == 0`, бейдж категории - по
  `cogs_known`. Расхождение 20 против 22 за июль. Проверять: каждая пользовательская подпись и её
  счётчик должны читать одно и то же поле.
- **Автор этой серии (свод Маркета) честно перечисляет, чего не проверял.** Три из четырёх названных им
  зон оказались дефектными. Вывод для калибровки: раздел «чего я не делал» - лучший вход в аудит, читать
  его первым и бить ровно туда.
- **Мой дрейф.** 6.55 против якоря 6.7 (реестр замечаний) - в допуске 0.15, класс вердикта тот же.
  Дрейфа не вижу. Тесты (220/220) и smoke (17/17) зелёные при четырёх FAIL по пробам - ещё одно
  подтверждение, что зелёный CI не является доказательством для класса «дашборд».

## Контур (kontur-dashboard), PR #358, итерации 1-2, 2026-09-22

- **Повторяющийся паттерн автора, третий раз за 2026: правка доводится до 2 из 3 поверхностей.**
  Ложное утверждение снято с двух экранов и осталось на первом (источник - массив в блоке данных, а не
  вёрстка экрана). Правило для следующего аудита класса «дашборд»: после правки текста грепать не по
  исходникам модулей, а по ОТРЕНДЕРЕННОМУ тексту всех экранов обоих режимов. Исходник врёт: строка жила
  в data-файле.
- **Класс дефекта, которого у меня не было: импорт измерения в модель без сверки с этой же моделью.**
  Автор притащил измеренную конверсию и не проверил, что звено выше по цепочке противоречит тому же
  счётчику в 10,7 раза (модельное подмножество больше измеренного целого). Проба на будущее: любую
  новую «измеренную» цифру прогнать через формулу, куда она вставляется, и сравнить результат с прямым
  измерением того же результата, если оно есть в репозитории.
- **Две модели атрибуции - две правды.** `ym:s:lastTrafficSource` против `ym:s:lastsignTrafficSource`
  дают по одному счётчику и одному периоду расхождение 26,8%. Если в репозитории два сборщика Метрики,
  всегда сверять, какой признак источника они спрашивают, прежде чем принимать «одну метрику».
- **git log как источник даты не воспроизводим в CI.** `actions/checkout@v4` по умолчанию depth 1, и
  `git log -1 -- <path>` отдаёт дату HEAD, а не дату файла. Проверяется за минуту:
  `git clone --depth 1 file://<repo>` и тот же git log. Добавить в пробы класса B.
- **Мой собственный bash-хук ложно срабатывает на `> 8` внутри питоновского кода и на `sed`-выражениях
  с `/d`.** Строковая эвристика видит перенаправление там, где его нет. Обходится литеральными путями и
  инверсией сравнения. Для Protocol 15: не дыра, а трение; стоит сузить шаблон.
- **Дрейф.** 6.00 против якоря 6.7 (реестр замечаний) - в допуске 0.7, класс вердикта тот же (return).
  Итерация 1 была 4.75 veto, дельта +1.25 за одну итерацию. Дрейфа вниз не вижу: три из четырёх
  блокирующих закрыты и проверены живьём, оценку держат рецидив на первом экране и два новых дефекта.

## Контур (kontur-dashboard), PR #358, итерация 3, 2026-09-23

- **Грепа по отрендеренному тексту недостаточно, если экран падает.** Я сам дал автору четыре шаблона
  для грепа, он честно их прогнал и получил нули. При этом весь его третий блокирующий пункт лежал на
  экране, который не рисуется: `TypeError` до возврата HTML, `innerText` пуст, греп по дампу даёт ноль
  ровно потому, что строк там нет. Правило: для класса «дашборд» сначала РЕНДЕРИТЬ каждый экран с
  перехватом исключения на экран, считать число упавших, и только потом грепать текст. Ноль совпадений
  и ноль контента дают один и тот же вывод грепа.
- **Зелёный сторож не заменяет исполнения экранов.** `check_dash.py` exit 0 и `tamper.py` 18 из 18 на
  коде, где `node tools/smoke.mjs` выходит с кодом 1. Сторож валидирует конверты и контейнеры графиков,
  он не вызывает функции экранов. Проверять в конвейере CI наличие шага, который реально исполняет UI.
- **Отчёт автора «проверено» проверять исполнением, а не чтением.** В сообщении коммита стояло
  «дымовой тест без горизонтальной прокрутки», тест на том же коммите падает с необработанным
  исключением. Стоимость проверки - одна команда. Делать это первым действием, до чтения диффа.
- **Ловушка: `exit=$?` после пайпа.** `node ... | tail -30; echo $?` печатает код `tail`, а не `node`.
  Дважды чуть не принял 0 за успех. Брать код отдельным прогоном с редиректом в файл.
- **Класс дефекта «общий текст на два разных состояния» не лечится правкой строки.** В итерации 2 я
  назвал конкретную строку блокеров, автор её заменил, и новая строка ровно так же врёт на втором
  проекте. Правило: если ложное утверждение пришло из общего на проекты массива или из статичного
  словаря правил, в rework_tz писать «разметить по проекту», а не «заменить текст».
- **Моя ошибка калибровки итерации 2.** Поставил `accuracy_1` = 2 за конверты, не прогнав прозу экранов
  на числа вне конвертов. В итерации 3 нашёл «Отказы 19%» против карточки ДАННЫЕ 17.1 на том же экране
  и вшитые «547/561 запросов» про чужой проект. Эти дефекты были и в итерации 2. Впредь: `accuracy_1`
  для дашборда ставится только после сканирования отрендеренной ПРОЗЫ на числа, а не только конвертов.
- **Починка может ухудшить факт.** Автор закрыл мой gap про дату файла проверкой `--is-shallow-repository`
  и выбросил дату, которую git в этом же репозитории отдаёт (флаг true, `git log -- <path>` -> 2026-09-16).
  Живой факт (97 целей) уехал в класс ГИПОТЕЗА. Правило для rework_tz: формулировать критерий приёмки
  через результат («дата верна в обеих средах»), а не через механизм («проверить глубину клона»).
- **Дрейф.** 5.55 против якоря 5.83 (Студия каруселей v4) - в допуске 0.28, класс вердикта тот же (veto).
  Серия 4.75 -> 6.00 -> 5.55. Снижение на третьей итерации объясняется одним дефектом (падение P0-экрана,
  контрфактически ~6.28), а не ужесточением планки. Проверено контрфактическим пересчётом в отчёте.

## Контур (kontur-dashboard), PR #358, итерация 4, 2026-09-23

- **Шифрование страницы не равно закрытым данным.** Проба, которая ломает за 10 секунд: открыть незапечатанную
  копию в публичном репозитории (raw.githubusercontent.com, HTTP 200). Для любого гейта на статике проверять ВСЕ
  копии защищаемого: файл на сайте, файл в git, данные, из которых он собран, и видимость репозитория.
- **Инструментировать помощник рендера, а не грепать текст.** Обёртка над val()/mini() со счётчиком вызовов с
  отсутствующим конвертом за один прогон нашла 12 вызовов (лист собственнику печатал прочерки при данных 144/19/29).
  Ни smoke, ни сторож, ни греп по дампу этого не видят: прочерк выглядит как честное «данных нет».
- **Проверка причинности заявленной починки.** Убрать одну строку правки в песочнице и воспроизвести симптом
  (killCharts: 66 ошибок SVG вернулись). Дёшево и отличает причину от подавления.
- **Мои пропуски итераций 1-3 (из поставки ae5a643):** прочерки на листе собственнику, «Видимость в ИИ 2500%»,
  конверсия 11% как ДАННЫЕ без файла, статичный «кабинет не подключён» на 10 экранах. Все четыре ловятся двумя
  пробами выше и проверкой единиц одной метрики на всех экранах. Впредь для дашборда: единицы и масштаб каждой
  метрики сверять по всем поверхностям, а не только значение.
- **Повтор класса у автора, четвёртый раз:** текст, не зависящий от состояния проекта (RD починен, METH и ветка
  yd-cost для второго проекта нет). В rework_tz писать «развести по состоянию все словари и ветки», с перечнем.
- **Дрейф.** 5.70 против якоря 5.83 (Студия каруселей v4), дельта 0.13. Серия 4.75 -> 6.00 -> 5.55 -> 5.70. Без
  моих пропусков автор узнал бы о половине дефектов раньше: это моя доля в том, что серия не сходится. Эскалировано
  Ивану, цикл return исчерпан.

## Контур (kontur-dashboard), PR #358, итерация 5, 2026-09-23

- **Правка, проверенная на одном снимке данных, откатывается на следующем.** Автор закрыл блокер признаком
  «клики direct.json равны кликам direct_metrika.json», на закоммиченном снимке это верно. Но файлы пишут два
  воркфлоу по разным расписаниям, а бот собирает раньше второго: реплей того же теста на истории main в час бота
  дал 1 утро из 14. Проба на будущее: любой признак, сравнивающий два источника, прогнать на истории main в час
  запуска бота (`git rev-list -1 --before=<ts> origin/main -- <file>`) и собрать код ветки на текущих файлах main.
- **Правило равенства кликов и фразу «окна сведены по длине» предложил я (rework_tz итерации 4).** Обе ошибки в
  артефакте мои. Мои предложенные механизмы проверяются на истории данных и расписаниях сборщиков до rework_tz.
- **Числа словами обходят греп по цифрам.** «двенадцати страниц», «Пять открытых вопросов», «За три дня»: B5 для
  дашборда гонять и по числительным (основы «пят», «двенадцат», «тр[её]х» и т.д.), а не только `[0-9]{3,}`.
- **Класс «текст не зависит от состояния проекта», пятый круг, пять новых мест.** Автор чинит названные места, не
  класс. В rework_tz давать проверку, а не список: каждое утверждение о состоянии (подключён, есть выгрузка,
  N вопросов, N дней) сверить с данными того же экрана на обоих проектах.
- **ДАННЫЕ без файла у главных чисел (план H2, пороги собственника) я пропускал пять итераций**, хотя за тот же класс
  (Roistat) снижал в итерации 4. B1 для дашборда начинать с самых крупных чисел, а не с попавшихся.
- **HITL по атрибуции.** Условное «если отвечал не Иван, действует после подтверждения» не есть решение Ивана:
  механизма ожидания нет, соседний документ уже пишет «принятый риск». Закрывает только действие, атрибутируемое ему
  (approve или комментарий в PR его учётной записью). Честность автора в формулировке не закрывает пробу.
- **Дрейф.** 6.35 против якоря 6.7 (реестр), дельта 0.35. Серия 4.75 -> 6.00 -> 5.55 -> 5.70 -> 6.35. Рост за счёт
  actionability (дата и числовой критерий у действия моста) и reversibility (RISK.md). Цикл return не сходится:
  каждый круг закрывает названное и открывает соседнее того же класса, решение за Иваном.

## Диалоги v3, итерация 3 (2026-09-22, score 8.0 return, класс гейт+дашборд)

- **Новая проба, которая дала больше всех: контрфактический прогон регулярки.** Не «прочитать лукбихайнд»,
  а собрать 4 варианта (OLD, без альтернативы A, без альтернативы B, shipped) и прогнать каждый по живым
  телам сообщений. Дало точную атрибуцию эффекта по каждой правке (86 против 26 сделок) там, где автор
  оперировал метрикой «единственный триггер» (150 против 23). Вносить в red-team-probes как B-пробу:
  при сужении или расширении любого матчера требовать эффект по каждому изменению отдельно.
- **Класс дефекта, которого у меня не было: защитный лукбихайнд шире мандата.** Признак: мандат называет
  N идиом, реализован blanket-гард по классу символов. Ищется разбором заблокированной популяции на
  категории. Здесь: описано 2 идиомы, заблокировано 4 класса, 24% блокировок вне описанной механики.
- **Класс дефекта: калибровочная обратная связь внутри одного релиза.** Изменение определения фактора
  меняет выборку закрытых сделок, по которой калибруются веса ДРУГИХ факторов, и двигает сделки, у которых
  набор факторов не изменился (здесь 44). Признак в коде: веса считаются в том же прогоне, что и факторы,
  и применяются ко всем строкам. Проверять всегда: diff блока calibration, а не только diff факторов.
- **Узкое заявление, которое читается как широкое.** «Рейтинги менеджеров не меняются» было буквально верно
  (rating и 6 разделов, 0 расхождений), но lossRub, tempDist и hotMoneyTemp тех же менеджеров поехали.
  Правило: на артефакте класса «оценка людей» проверять не заявленное поле, а ВСЕ поля объекта менеджера.
- **Точная метка ломается громче размытой.** Смена текста «без быстрого ответа» на «без ответа» превратила
  размытое утверждение в проверяемое, и оно оказалось ложным на 2.2% случаев из-за MIN_ANSWER_LEN=25.
  При framing-правках всегда спрашивать: держит ли детектор ту силу утверждения, которую даёт новый текст.
- **Мой дрейф.** 8.0 против якоря 8.1 (реестр после доработки) - в допуске 0.1. Автор воспроизвёл каждую
  свою цифру и приложил self-check; проверка байт-в-байт сошлась с обоих концов (база и пересборка).
  Держать планку помог не weighted (8.0 = go), а Hard Rule 9 по FAIL пробы класса A.
- **Хук feniks-bash-scope: 4 ложных срабатывания за сессию** на легальных записях в /tmp (стрелочная функция
  в аргументе node, sed -i, путь через переменную). Fail closed, но heuristic не видит cd и не раскрывает
  переменные, поэтому резолвит относительные пути от корня проекта.

## Диалоги итерация 3, РАУНД 2 (2026-09-22, return 7.85 после return 8.0)

- **Главный урок про формулировку проб. Автор чинит тест-кейс, а не класс.** В раунде 1 я записал пробу A4
  как две строки: «за  счет» (два пробела) и «за\nсчет». Автор закрыл ровно их (`\s{1,3}`) и объявил пробу
  закрытой. Открытыми остались 4 и более пробелов, 4 переноса, «засчет» слитно, «за-счет», ZWSP, «нассчет».
  Правило: пробу формулировать как КЛАСС обхода с батареей входов, а не как найденные примеры, и в rework_tz
  писать корректную форму (здесь `(?<!за\s*)`), иначе автор закроет примеры и напишет «проба закрыта».
  Это третий случай того же паттерна в памяти (см. строку 17 про ложные срабатывания).
- **Класс дефекта: асимметрия замера.** Автор измеряет точность там, где ошибка дешёвая (снятие штрафа,
  разбор 14 из 111), и утверждает без замера там, где ошибка дорогая (добавление штрафа, 408 реплик,
  ноль замеров). Это та же ошибка, что я снял в раунде 1, развёрнутая в обратную сторону. Признак:
  категоричное «остальные N законные» рядом с аккуратной выборкой по другому изменению. Проверять обе
  стороны размена всегда, в том числе когда направление правки совпадает с моим доводом прошлого раунда.
- **Считать поверхности метрики grep по коду, а не по объекту данных.** В раунде 1 я назвал четыре
  поверхности «мяча» и пропустил пятую: trend.json считает сырой предикат (score-dialog.ts:999), поэтому
  график и таблица расходятся на 256 сделок про один день. Искать не «где метрика показана», а «где
  предикат вычисляется заново». Моя недоработка, не авторская.
- **Смотреть, КТО получает обвинение, а не сколько обвинений.** 75 процентов меток сидели на лидах,
  80 процентов на сущностях с нулевым бюджетом, часть на прямом спаме. Число меток выглядит умеренно,
  популяция нет. На артефакте, который оценивает людей, разбивать метрику по типу сущности и наличию
  денег ДО оценки величины.
- **Проверять соседние коммиты автора, а не только названный.** Между раундами лёг третий коммит,
  удаливший из шаблона очередь, на которую ссылается приёмка и которую защищает раздел «осознанно не
  сделано». Смотреть `git log base..head`, а не `git show` названного коммита.
- **«Закрыт в корне» против «не сработал в этот раз».** Калибровочный канал дал 0 сделок против 44,
  но выборка (n 168 в 172) и сырой вес (raw 0.34 в 0.33) всё равно поехали, обратно на 0.66 приземлило
  демпфирование. Проверять не итоговый множитель, а n и raw: если они двигаются, механизм цел.
- **Мой дрейф: 8.0 в 7.85 при артефакте, который материально улучшился.** Снижение пришло целиком из
  risk_awareness (7.0 в 6.0): два ложных заявления о закрытых защитах и инструкция отката для старой
  головы. Accuracy и downside при этом выросли. Записываю явно, чтобы снижение не читалось как общее
  ужесточение планки. Якорь 8.1 reestr, расхождение 0.25, в допуске.
- **Хук feniks-bash-scope: 5 срабатываний, 4 ложных** (стрелочная функция в аргументе node, `open(...)`
  в python-однострочнике, путь через переменную, heredoc со стрелочной функцией и со стрелкой «в» между
  числами), 1 верное (`git checkout --`). Честный обход: скрипт в /tmp через Write, затем запуск одной
  командой без перенаправлений. Эшелон живой, fail closed.

### Диалоги v3, итерация 3, раунд 3 (2026-09-22, go 8.1)

- **Я был неправ дважды в одном аудите, и оба раза автор доказал это данными.** (1) Предложенный мной гард
  `(?<!на\s*)сч[её]т` глушит не предлог, а две последние буквы предыдущего слова: «нужна счет-фактура»,
  «должна счет прислать», «нам нужна счет на оплату». В корпусе он снял бы 16 реплик сверх нужного, семь
  из них живые вопросы клиента. (2) Моя рекомендация «не ставить bad-метку лидам и сущностям с бюджетом 0»
  спрятала бы самый дорогой сигнал: в моей же выборке 23 из 34 обвинённых лидов - живые входящие заявки.
  Правило: предлагая регулярку, прогонять её по корпусу ДО того, как писать в rework TZ. Предлагая
  blanket-фильтр по типу сущности, сначала считать долю живых внутри этого типа.
- **Классифицировать тон метки по полю `tone`, а не по тексту.** В раунде 2 я дал базе «bad 0», потому что
  искал `/без ответа/`, а база писала «Горячая реплика без быстрого ответа». Реальная база: 601 bad на
  461 сущности, 36.01 млн руб. Из-за этого мой тезис «бюджет под обвинением вырос на 74%» сравнивал раунд 1
  с раундом 2, а не релиз с базой; против базы релиз снижает обвинения втрое. Базовую линию брать из того же
  поля, что и текущую, и всегда сравнивать с тем состоянием, которое живое.
- **Чекпоинт 1 (метки цифр) проверять grep, а не впечатлением.** Два раунда подряд закрывал его на 2/2,
  а действующие разделы методички шли вообще без [ДАННЫЕ]/[ГИПОТЕЗА] (7 меток на файл, все в разделе
  раунда 1). Accuracy раундов 1 и 2 была завышена на 1.0. Команда: `grep -n "\[ДАННЫЕ\|\[ГИПОТЕЗА" <файл>`
  и сверить номера строк с разделом, который сейчас действует.
- **Новый гейт проверять на ВСЕХ поверхностях сущности, а не в том цикле, куда его поставили.** Фильтр
  рассылок снял 64 сущности из метрики горячих реплик, но все 64 сохранили фактор `ball 0.7` и bad-тег
  «Без ответа N ч», а 8 - ещё и «клиент напоминает сам». Обвинение не снято, а переименовано. Проба:
  после фильтра выгрузить те же сущности и посмотреть факторы и теги целиком, а не только целевую метрику.
- **Маркерные списки: спрашивать «сколько отправителей из скольких».** Список из семи фраз закрыл двух
  вендоров, три фразы не сработали ни разу, а 22 из 135 обвинённых лидов остались машинной почтой
  (Feihong 8, продажа баз 4 и далее). Устойчивый ключ для машинной почты - домен отправителя, не деловая
  фраза: «коммерческое предложение от» отличается одним предлогом от живой заявки «коммерческое
  предложение с ценой и сроками» (68 таких реплик, 26 сущностей с деньгами).
- **Приёмка: открывать шаблон и искать колонку.** Второй раунд подряд приёмка ссылается на UI, которого
  нет (раунд 2 - удалённая очередь, раунд 3 - «фильтр по колонке Разметка», которая живёт в ленте
  сообщений, а не в таблице сделок). Команда: `grep -n "DCOLS\|ECOLS" <template>` и сверить уровень.
- **Калибровка серии: 8.0, затем 7.85, затем 8.1.** Рост дали risk_awareness (6.0 в 8.0: обе опровергнутые
  защиты закрыты) и insight (8.0 в 9.0: три альтернативы отвергнуты данными). Accuracy упала с 8.0 до 7.0
  не из-за артефакта, а из-за моего исправления чекпоинта 1. Когда снижаю подкритерий собственной
  поправкой, писать это в отчёте явно, иначе автор читает как ужесточение планки. Якорь 8.1 reestr,
  расхождение 0.0.
- **Хук feniks-bash-scope, третий раунд: 3 срабатывания, все ложные** (путь через переменную, стрелочная
  функция в аргументе node, стрелочная функция внутри heredoc). Устойчивый обход - Write скрипта в /tmp
  и запуск без перенаправлений. Накопленная статистика серии: 12 срабатываний, 11 ложных, 1 верное.
  В Protocol 15: heuristic не раскрывает переменные и не отличает стрелочную функцию от перенаправления.

### Градусник Диалогов v2 (2026-09-22, return 6.65, класс дашборд, та же серия)

- **Проба, которая дала больше всех: тождество метрики.** Не «поменялись ли числа», а «не схлопнулась ли
  формула». Постоянная точка отсчёта превратила «медиану Δ» в «медиану уровня минус константа»
  (0 нарушений тождества на 5050 строк). Класс дефекта: правка убирает смещение, заменяя его зеркальным.
  Признак в коде: одна из двух величин в разности стала константой. Проверять всегда, когда «нормализуем
  точку отсчёта».
- **Вторая по силе: ранговая корреляция метрики с тем, что она НЕ должна мерить.** Spearman(метрика,
  состав портфеля по стадиям) = 0.943 и 1.000. Дешевле любой калибровки и читается однозначно. Вносить
  в red-team-probes как B-пробу для класса «оценка людей»: взять величину, от которой метрика по замыслу
  не должна зависеть, и посчитать ранги.
- **Класс дефекта: критерий приёмки проверяет форму, а не правильность.** «Разброс не менее 10° и хотя бы
  один в минусе» выполняется любой шкалой, и выполнился тем, что в минусе оказался лучший закрывающий
  (57.3% при n=150). Второй критерий («driftAlso длиннее двух пунктов») недостижим по данным (максимум 2).
  Правило: приёмку читать как тест, который можно провалить, и искать вход, на котором она провалится.
- **Повтор замечания прошлой итерации, второй раз в одной серии.** «Рейтинги 0 из 19» снова верно буквально
  и снова узко: поехали tempDist, heatPower, probAvg, lossRub у 19 из 19, очередь lowprob и topLoss в
  рублях. Плюс повтор про нерабочую инструкцию отката. Когда автор повторяет замечание, которое я уже
  выдавал в этой серии, это не accuracy, это risk_awareness: механизм проверки у автора не появился.
- **Заявленное направление правки проверять на деньгах отдельно.** «Смягчение» было верно для градуса и
  неверно для рубля: lossRub вырос на 3.3%, topLoss «тишина» у одного менеджера на 27%. Механика: штрафы
  мультипликативны, при росте prob предельная рублёвая цена оставшегося штрафа растёт.
- **Self-check закрывает чекпоинт ссылкой на содержимое, которого в артефакте нет** (пункт 13, альтернативы)
  и ссылкой на документ, в котором темы нет вовсе (пункт 3, semantic-док без единого упоминания градусника).
  Команда: для каждого «да» в self-check найти абзац в теле артефакта; для каждой ссылки на RAG сделать grep.
- **Четвёртый раз в серии между брифом и вердиктом лёг новый коммит** (7ef1359 во время аудита, трогает ту
  же метрику). Перепроверять ключевые дефекты на новой голове и писать это в отчёт отдельным разделом.
- **Мой дрейф.** 6.65 против якоря 6.7 reestr, расхождение 0.05. Против моей же оценки предыдущей итерации
  той же серии (8.1) падение 1.45, источник: risk_awareness 8.0 в 4.0 и insight 9.0 в 6.0. Записал в отчёт
  явно, чтобы автор не читал как общее ужесточение. Три из четырёх вопросов брифа («не завышен ли вес
  first_fast», «обоснованно ли исключение Долгостроя», «верен ли состав семьи дрейф») закрылись в пользу
  автора на данных, это тоже держит планку честной.
- **Был неправ в предположении до проверки.** Шёл проверять «не завышены ли веса вверх», нашёл обратное:
  1.1 при лифте 1.48x и 1.15 при лифте 2.3x скорее занижены, а проблема в пороге (15 мин разделяет хуже,
  чем 60 или 240). Правило: на вопрос «не завышено ли» считать лифт по исходам, а не смотреть на число
  срабатываний.
- **Хук feniks-bash-scope, четвёртая сессия: 5 срабатываний, 4 ложных** (стрелочные функции в heredoc,
  `2>/dev/null` после относительного пути, `<` для stdin у patch, `q.key===` в однострочнике node),
  1 верное по классу (`git apply --check` заблокирован как git-запись, хотя это dry-run). Накопленная
  статистика: 17 срабатываний, 15 ложных. Устойчивый обход честный: Write скрипта в /tmp, запуск без
  перенаправлений, флаг `-i` вместо `<`.

## Калибровка 2026-09-23: база налога в том же своде Маркета (дашборд, iter1, 6.15 return)

- **Проба B4 сработала второй раз подряд на одном артефакте.** 16.09 расходились две вкладки, 23.09 -
  две страницы («Деньги» против «Товаров»). Правило укрепилось: в мультистраничном дашборде метрику
  надо гонять по ВСЕМ страницам, где она есть, а не только там, где её правили. Дешёвый способ
  найти механизм разрыва: взять подсказку водопада, где названа сумма краевого расхода, и сравнить
  её с дельтой между поверхностями - совпало до рубля дважды.
- **Новый класс дефекта: эталон сверки есть вторая копия проверяемой формулы.** Признак в коде теста -
  функция `ref()`, которая читает тот же снимок и повторяет расчёт. Тест зелёный, обе копии несут одну
  слепую зону, а в коммите это подаётся как «сверка с эталоном» по §15. Ищется так: открыть тест,
  на который ссылается DoD, и спросить, откуда в нём берётся правая часть равенства. Если из того же
  файла данных плюс арифметика автора - это не эталон.
- **Цифры в коммите могут не сходиться сами с собой, и это ловится без единой пробы.** Заявленная
  рентабельность делится на заявленную чистую и поступление за тот же период; разошлось на 0.70 п.п. -
  дальше уже понятно, куда копать. Делать этот арифметический тест ПЕРВЫМ, до сборки страницы:
  он стоит одну минуту и в этот раз указал на главный дефект.
- **Разделять механику и семантику в оценке.** Здесь механика сильная (база воспроизведена вне
  артефакта по 16 парам с нулём), а правомерность самой величины не подтверждена ничем. Соблазн
  поставить выше за качество исполнения - это и есть дрейф. Оценку держит вопрос «откуда взялось
  правило», а не «насколько аккуратно оно посчитано».
- **Повторяющийся разрыв этого автора (третий аудит подряд):** сверка делается тщательно, но против
  источника, выбранного самим автором. 16.09 - итог против самого себя, 23.09 - «Товары» против копии
  формулы. При этом там, где автор берёт действительно внешний источник (выгрузка заказов), всё сходится
  до рубля. Значит, дефект не в аккуратности, а в определении слова «эталон». Бить сюда первым делом.
- **Протокол 12 в дашбордах.** Изменение правила расчёта по устной просьбе, противоречащее эпизоду,
  сверенному с Иваном, - самостоятельный gap, даже если число посчитано верно. Проверять на каждом
  аудите цифр: есть ли в `knowledge/` запись, которую эта правка молча отменяет.
- **Мой дрейф.** 6.15 против ближайшего якоря 6.55 (тот же дашборд, неделей раньше) - дельта 0.40,
  класс вердикта тот же. Дрейфа не вижу. Тесты 461/461 и smoke зелёные при одном FAIL и трёх PARTIAL
  по пробам: подтверждение прежнего вывода про зелёный CI.
- **Хук feniks-bash-scope, ложные срабатывания за сессию: 2.** Оба на heredoc с python (регулярка со
  слэшами и список чисел через запятую читаются как цели записи). Обход прежний и честный: Write в
  /tmp, затем запуск файла. Стоимость - 2 хода.

## Контур (kontur-dashboard), PR #358, итерация 6, 2026-09-23

- **Регресс защиты через лечение ложного срабатывания, третий раз в 2026** (после ym-dashboard aggregate/growOn).
  Кит DS принёс в открытый CSS слово, совпавшее со строкой данных; автор вырезал все комментарии до поиска утечек,
  и проверка перестала видеть имя цели в HTML-комментарии, которое ловила итерация 5. Проба A5r: одна фикстура на
  seal@старый и seal@новый. Узкое лечение: исключать точный текст файлов пакета по хэшу, а не класс синтаксиса.
- **Склейка рядов прячет разрыв.** gaps() по ряду, склеенному с ретроспективой выгрузки, пишет «Ряд без разрывов,
  съём по регламенту», хотя своих съёмов два за 16 дней и бота нет в main. Проба: индикатор свежести и полноты
  считать отдельно по каждому источнику ряда и сверять с инструментом репозитория (snapshot.py --check).
- **Смена стека убивает правила сторожа молча.** OPACITY ловит классы Tailwind, которого больше нет; случай tamper
  проверяет мёртвый класс и зелёный; замер контраста не учитывает opacity. Проба при миграции UI: каждое правило
  нарушить способом НОВОГО стека (CSS opacity, rgb(), 3-значный hex).
- **Согласование числа и слова строкой ломается на обновлении данных** (144 -> 147 заявки). Скан согласования по
  дампу - одна команда, гонять всегда после смены данных.
- **Мои пропуски итерации 5:** 6 из 12 дефектов CG и факты карты посадочных как ДАННЫЕ без документа. Причина:
  читал P0 выборочно, B1 начинал с крупных денег. Впредь: B1 по всем конвертам ДАННЫЕ с источником-документом
  (`git log --all -S<число>`), CG по всем P0 обоих проектов целиком, до первой оценки.
- **Дрейф.** 6.60 против якоря 6.7 (реестр), дельта 0.10. Серия 4.75 -> 6.00 -> 5.55 -> 5.70 -> 6.35 -> 6.60.
  Рост только за accuracy_3 (C1 закрыт и проверен на 31 утре). Цикл не сходится и по моей вине: каждый круг я
  нахожу старые дефекты, которых не назвал раньше. Решение о продолжении цикла за Иваном.

## Контур (kontur-dashboard), PR #358, итерация 7, 2026-09-23

- **Слоистый гейт проверять выбиванием слоя.** Автор добавил первый слой (страница равна эталону из исходников до
  символа), и все мои фикстуры итерации 6 (правки готовой страницы) стали ловиться им тривиально. Тест на подмену
  зелёный 24/24 и без всего второго слоя. Проба: убрать каждый слой по очереди и прогнать тест. Если тест зелёный,
  слой не проверяется ничем, и его регресс (здесь исключение блоков пакета по id) проходит молча.
- **«Равно файлам пакета» при эталоне из тех же файлов это круговое доказательство.** Исключение из поиска по id блока
  есть исключение класса. Работает только закреплённый хэш версии. Мой критерий итерации 6 был через фикстуры
  готовой страницы, механизм «по хэшу» стоял в тексте и не был критерием. Впредь в критерий гейта включать фикстуры
  того пути, который останется после починки (здесь утечка в исходниках с пересборкой).
- **Метка в подсказке не доезжает до печати.** ГИПОТЕЗА у фактов дошла до данных и двух экранов, а на листе
  собственнику с кнопкой «Печать» класс живёт только в data-tip. Пятый раз «правка на 2 из 3 поверхностей». B7 для
  дашборда начинать с поверхностей экспорта (печать, лист, выгрузка) и смотреть видимый текст, а не DOM-атрибуты.
- **Решение о сроке расходится по экранам, если сроки вшиты строками в модули.** Ответственный и срок из ответа
  владельца попали на 4 экрана, на «Плане и факте» остался «срок не назначен». Грепать дамп по названию задачи и
  сравнивать «кто / когда» на всех экранах.
- **Правила сторожа снова закрыты по моим примерам** (третий раз): COLOR ловит #F0A и rgb(), пропускает hotpink и hwb();
  прозрачность ловится как opacity, пропускается как filter: opacity() и color-mix(... transparent).
- **HITL без имени.** Ответ владельца сессии без имени, записанный агентом, считаю PARTIAL: решение есть, атрибуции нет.
  Закрывает одно действие Ивана в PR. Цену цикла (невосстановимые точки съёма) показывать вместе с развязкой.
- **Мой пропуск:** коды кластеров на P0 «Что блокирует» были в C2 итерации 6, в дефекты CG я их не вынес.
- **Хук feniks-bash-scope, ложные срабатывания: 5** (curl -o и -w, git clone в /tmp, cp с переменной пути, sed с «|»,
  python -c со знаком «=»). Обход прежний: скрипт в scratchpad через Write, литеральные абсолютные пути.
- **Дрейф.** 6.85 против якоря 6.7 (реестр), дельта +0.15. Серия 4.75 -> 6.00 -> 5.55 -> 5.70 -> 6.35 -> 6.60 -> 6.85.
  Рост за actionability (ответственный и срок подтверждены). Держат B7, B4 и CG, все три одного класса «правка не на всех поверхностях».

## Контур (kontur-dashboard), PR #358, итерация 8, 2026-09-23

- **Где я был неправ (итерация 7).** «Точки съёма невосстановимы, keys.so задним числом не отдаёт» я вывел, не открыв
  git-историю выгрузки. harvest коммитит keysso.json в main, в истории по 12 коммитов на проект за 02.09-22.09 с топ-50
  и ответами ИИ. На этой посылке стояли мой gap 9 и п.2 итерации 7 (развязка через PR #401); автор и СПАРТАК приняли её
  без проверки. Правило: прежде чем писать «невосстановимо», пройти git log по файлу выгрузки в main и git show по коммитам.
- **Пробы, которые нашли дыры.** Печать листа в PDF (page.pdf A4 и pdf.js) с подсчётом страниц: обещание «один лист»
  против двух. Дубль id закреплённого блока: хэш проверяется у первого совпадения, из поиска вырезаются все. Теги meta
  вне поиска значений. iframe srcdoc как скрипт без тега script: живьём исполняется до пароля и читает ключ сессии.
  Для гейтов со списком запретов по строкам начинать с тегов, которые исполняют код без script.
- **Буквальная строка в тесте маскирует регресс.** seal_test ищет «240 000 000» в результате, и любая моя фикстура с этой
  строкой ловится тестом, а не шифровальщиком. Фикстуры гейта строить без строк, которые тест ищет буквально, и смотреть
  вывод шифровальщика отдельно от шага.
- **Повтор у автора, шестой круг: правка по процитированной строке, а не по классу.** «В разы» снято в разделе 1 листа,
  осталось в разделе 3 того же листа и на четырёх экранах. Критерий CG писать как grep по дампу P0 («в разы» = 0), а не
  как замену одной фразы. Self-check автора завышает brand_16, 19, 20 («да / частично» против моих 1 / 1 / 0).
- **HITL.** Комментарий в PR, написанный агентом от учётной записи владельца репозитория и честно помеченный «не Иван»,
  атрибуцию не даёт. A1r остаётся PARTIAL, пока нет действия человека.
- **Хук feniks-bash-scope, ложные срабатывания: 6** (знак >= внутри node -e, угловые скобки в шаблоне grep, python open()
  на чтение файла проекта, URL у curl, sed -i в /tmp, переменная в пути). Обход прежний: скрипт в scratchpad через Write,
  литеральные абсолютные пути, инструмент Grep для шаблонов с угловыми скобками.
- **Дрейф.** 7.10 против якоря 6.7 (реестр), +0.40. Серия 6.60 -> 6.85 -> 7.10. Рост за accuracy (B7 и B4 закрыты
  исполнением). Держат CG листа собственнику, B1h (ложная посылка, моя доля) и HITL без Ивана.

## 2026-09-24 · дашборд ПТО (5.55 veto) - калибровка

- **Новая обязательная проба для класса «дашборд/цифры»: подбор окна.** Если артефакт объявляет
  эффект на ОДНОМ определении периода (последние N дней, декада, неделя) - прогнать сетку всех
  разумных определений и посмотреть, где в ней лежит объявленное. Здесь выбранное окно оказалось
  минимумом p из 14, а глобальный минимум вообще у середины месяца (0.0556 против 0.0612 у конца).
  Стоимость пробы - один скрипт на 40 строк. Ловит подгонку даже когда вывод автора отрицательный:
  опасна не подгонка к «да», а рамка «почти дотянули», которая читателя разворачивает.
- **Вторая новая проба: разрыв режима внутри окна анализа.** Смотреть не только на пропуски в данных,
  но и на СТРУКТУРУ источника: удалённые стадии, переименования, миграции. Здесь стадия с 1545
  переходами умерла 23.07 посреди апрель-августа; ни одна половина периода не значима, значимость
  появляется только в пуле. Артефакт подал это как косметическую проблему с названием стадии.
  Правило: удалённая сущность объёмом больше исследуемой - это не подпись, это структурный разрыв.
- **Третья проба: та ли популяция.** Считать распределение времени жизни в стадии прежде, чем верить
  счётчику событий. 59.5% «запусков расчёта» покидали стадию в тот же день, 275 из них сразу в
  предоплату. На подвыборке реальной работы эффект исчезал (p 0.26 против 0.061). Медиана 0 дней
  стояла на самой странице отдельной ячейкой и ни с чем не была связана.
- **Гейт репозитория как готовый инструмент пробы B7.** Быстрый и убедительный приём: взять grep-гейт,
  которым CI уже защищает СОСЕДНИЕ страницы, и прогнать его по новому файлу. Здесь `ROSTER` плюс
  `crm/deal/details` из deploy-pages.yml сработали (801 вхождение), а на пути этого файла гейта нет.
  Это сильнее рассуждения про PII, потому что критерий задан не мной, а самим репозиторием.
- **Проверять заявленный путь деплоя grep'ом, всегда.** README описывал шаг «Build PTO dashboard»,
  которого в workflow нет вообще (`grep -i pto` = 0 строк). На этом же выдуманном шаге стояли пункты
  self-check 24 и 25. Одна команда закрыла два чекпоинта сразу.
- **Где я чуть не переоценил.** Арифметика воспроизвелась до рубля и до 4-го знака p (порт mulberry32
  на Python дал ровно 0.0630), автор сам нашёл у себя переполнение double в LCG и это задокументировал.
  Соблазн поднять оценку за качество исполнения - тот же дрейф, что 23.09: механика сильная,
  правомерность выводов не подтверждена. Держал разделение, оценка осталась 5.55.
- **Гипотеза, которая НЕ подтвердилась, и это записано в отчёт.** Версия «хвост месяца набит
  прокликиваниями» выглядела правдоподобно: доля нулевых внутри хвоста 58.0%, снаружи 56.1%.
  Разницы нет. Отрицательные проверки писать в отчёт явно, иначе автор повторит их сам.
- **Мой дрейф.** 5.55 против якоря 5.83 (карусели v4) - дельта 0.28, класс вердикта тот же; против
  6.7 (реестр) - 1.15, оба в пределах 1.5. Дрейфа не вижу.
- **Хук feniks-bash-scope, ложные срабатывания за сессию: 4.** Питоновское сравнение «больше-равно N»
  в heredoc прочитано как редирект в файл; `curl -w` с фигурными скобками и `2` с амперсандом-единицей
  тоже; `python3 -c` со строкой `s=open(...)` тоже; четвёртое - на тексте ЭТОГО же пункта памяти.
  Плюс `git worktree add` из инструкции заказчика заблокирован как git-запись (обход честный:
  `git archive` в конвейер с `tar -x -C /tmp`, чистое чтение). Стоимость за сессию - 5 ходов.
  Рабочая схема прежняя: Write в /tmp, затем запуск файла питоном.
- **Ловушка окружения, не артефакта.** `git fetch --depth=1 origin <ветка>` с первого раза оставил
  FETCH_HEAD от прошлой сессии (указывал на main), и первые команды читали не ту ветку: в дереве
  не нашлось ни одного файла артефакта. Признак - `git ls-remote origin refs/heads/<ветка>` не
  совпадает с `git rev-parse FETCH_HEAD`. Сверять эти два SHA до чтения артефакта, всегда: иначе
  аудит идёт по чужому дереву, и по пустому результату это выглядит как «автор ничего не положил».

## 2026-09-24 - PR #403 analytics-mvp: плато на паре, pairFit, слияние соинвеста (6.55 return)

- **Приём, сработавший лучше всего: пересчитать «подобранный по факту» порог на РАЗНЫХ значениях скрытого
  параметра.** Автор защищал пороги pairFit словами «любая граница между 7.1 и 25.2 даёт одно и то же
  разбиение». Разбиение устойчиво к порогу, но НЕ к окну: окно 14 зашито в default, при 21 разбиение
  меняется на 1 пару из 4, на полной истории на 2 из 4. Правило: если порог стоит на статистике, которая
  монотонно растёт с длиной окна (размах, максимум минус минимум, сумма), порог обязан быть нормирован на
  окно, а окно названо и покрыто тестом. Проверять сдвигом параметра, а не спором о величине порога.
- **Второй приём: считать ИЗМЕНЕНИЕ, когда автор доказывает уровнем.** Довод «дни общего сдвига в парном
  ряду не дисквалифицируются, потому что 21.09 разница в парах осталась около нуля» разбился, как только
  посчитал изменение против последнего дня до старта: минус 2.1, плюс 0.4, минус 21.3, минус 7.1. «Уровень
  около нуля» и «не двинулось» - разные утверждения. Постоянная проверка для любого difference-in-differences.
- **Третий приём: искать в репозитории ВТОРОЙ источник той же величины.** Блок выводил наружу «рекламного
  расхода до старта не было вовсе» по файлу с 03.07, а тот же скрипт держал файл с 04.02, где расход есть.
  Ложно для 8 из 13 названных артикулов. Правило: абсолютное «никогда, не было вовсе» проверять по самому
  длинному доступному источнику, а не по тому, который читает код.
- **Новый класс дефекта для чек-листа: блок аргументации применён ко всем сущностям, а написан под одну.**
  adsContinuity цитирует зашитую гипотезу теста 1 на карточке теста 2 и печатает вывод «дизайн не может
  ответить на свой вопрос» про вопрос, которого в тесте 2 нет. Искать grep'ом зашитые цитаты внутри
  функций, вызываемых в цикле по сущностям.
- **Провенанс теряется при слиянии данных, и в диффе кода это не видно.** Инструмент слияния ставил метку
  источника константой и не читал поле источника из среза; метка exact исчезла из данных целиком (498 строк
  стало 0), из-за чего страница сдвинула дату начала наблюдения витрины на 14 дней назад и напечатала ложь.
  Смотреть распределение полей-провенансов до и после (git show базового коммита против головы), а не только
  диффы кода. Дало три gap'а из десяти.
- **Ключ-строка вместо ключа-идентификатора.** Участие в акции по названию читается как выход при
  переименовании. Доказательство лежало в самом файле: на единственных двух днях одна акция переименовалась
  на 251 артикуле. Правило: если ключ это человекочитаемая строка от внешней системы, искать переименования
  ВНУТРИ имеющихся данных, а не рассуждать о гипотетических.
- **Мой дрейф.** 6.55 против якоря 6.7 (реестр) - дельта 0.15; против прошлой итерации серии (6.65) - 0.10
  вниз, при том что PR закрыл реальные дефекты. Обосновал: закрытые дефекты компенсированы новыми ложными
  утверждениями на публикуемой странице. Дрейфа не вижу.
- **Хук feniks-bash-scope, ложные срабатывания за сессию: 4.** Чтение через pathlib в одной команде с
  записью в /tmp; open с параметром кодировки (чтение) рядом с вызовом записи; оператор сравнения
  «больше-равно» прочитан как перенаправление (третий раз за три сессии, это системная слепая зона);
  четвёртое - на тексте ЭТОГО пункта памяти, из-за угловых скобок и знака равенства. Стоимость 4 хода.
  Рабочая схема: конвейер с read-only головой (cat файл в python3 минус c) либо Write в /tmp и запуск файла.

## 2026-09-24 - разбор вкладки «Деньги» OZON: артикулы против заказов (дашборд, iter1, 6.15 return)

- **Новый класс дефекта: автор меряет разницу между двумя подмножествами и выдаёт её за ошибку каждой
  таблицы.** «По заказам завышено на 2,7 млн» было вкладом летящих заказов целиком, а ошибка на своём
  базисе - только их недоначисленные сборы (около 1,95 млн). «По артикулам занижено на 1,5 млн (СС без
  выручки)» считало не ту сторону дыры: не хватало К выплате ранних заказов, занижение 2,54-2,66 млн.
  Проба, которая это ловит: контрфакт на копии данных страницы - вернуть недостающее в ту таблицу, где его
  нет, и пересчитать итог её же формулой. Здесь знак сентября развернулся с -1,48 млн на +1,06...+1,19 млн.
- **Независимый ключ для «по какой дате лежит» - отчёт с двумя датами на заказ** (payout_orders: дата заказа
  и дата начисления). 228 из 228 дискриминирующих заказов решили вопрос, который месячные суммы решить не
  могли: суммы совпадали потому, что оба файла из одного вызова postings. Проверять происхождение обоих
  концов сверки grep'ом по продьюсерам (кто вызывает какой эндпоинт) до того, как принять «сходится».
- **B4 снова дал больше всех.** Главный визуал страницы (водопад, первый блок) строится на третьей
  таблице, а автор разбирал две. Правило: на вкладке с водопадом всегда читать, из какого ИТОГО он
  строится (`ordGrand`), и снимать его последний бар живьём.
- **B2 на части ЗАКРЫТОГО месяца - дешёвая и результативная.** Половины августа разошлись с целым на
  -227 078 из-за базы АДМ «только SKU с реализацией больше нуля». Признак в коде: база налога или АДМ
  фильтруется по знаку или по наличию штук на строке - значит итог не аддитивен по периоду.
- **Повтор паттерна «сверка против источника того же семейства»** (четвёртый раз в сентябре): «Начислено =
  P&L канала» - один эндпоинт транзакций, «Реализовано = отчёт» - по построению. Бить сюда первым.
- **Правило self-check.** Отчёт без self-check оценён, как в аудитах 2026-09-17 и 2026-09-21, потому что Иван
  прямо просил сверку с документами. Это расходится с буквой CLAUDE.md §4. Вынести Ивану: оценивать ли
  без self-check, если просьба об аудите исходит от него самого.
- **Мой дрейф.** 6.15 против якоря 6.7 reestr, дельта 0.55, класс вердикта тот же. Чекпоинтная сумма
  сначала дала 5.90 (veto) при actionability_6 = 0; поднял до 1, потому что РОМАН назван владельцем
  двух сверок. Ядро отчёта верное и подтверждено на уровне заказа, маркеров veto (нарушение доверия) нет.
- **Хук feniks-bash-scope: 4 ложных срабатывания за сессию** (sed -i по относительному пути после cd;
  awk с условием на номер строки; python со сравнением с дробным числом; sed, в тексте замены которого
  стояли кавычки-ёлочки и запятые). Обход прежний: Write скрипта в scratchpad, запуск файла.

## 2026-09-25 · PR #407 OZON «Деньги» после решений Ивана 25.09 (6.90 return, дашборд)

- **Мой дрейф.** 6.90 против якоря 6.7 reestr (+0.2), класс вердикта тот же. Прошлый аудит вкладки 24.09 - 6.15;
  рост за счёт сверки закрытых месяцев с независимым xlsx «Начисления» до рубля (2645 из 2645 отправлений).
- **Проба, которая нашла дыру: фикстура «сборщик молча упал».** Копия data в /tmp, обрезать накопительный файл
  (realization_daily) по дате, пересобрать с DATA_DIR/OUT_DIR в /tmp, снять ИТОГО Playwright'ом. Нашла fail-open:
  минус 1,2 млн за неделю без плашки (сборщик выходит с кодом 0, у API окно 32 дня). Обязательна для любого
  накопительного источника с окном хранения у API.
- **B4 на ОТКРЫТОМ месяце.** Автор проверил «ИТОГО = водопад = план-факт» только на закрытых месяцах. План-факт
  (monthTotals до конца месяца) видел день, которого таблица не показывает (MAXD страницы из history, деньги из
  другого файла): 659 017 против 712 120. Признак в коде: MAXD и данные денег из разных файлов.
- **Подписи после смены источника.** Водопад перевели на другую таблицу, подпись #src1 осталась от старой. Правило:
  при смене источника визуала grep по старому названию источника во всех подписях карточки.
- **Паттерн автора серии OZON «Деньги»:** правка дохода без симметричной правки расхода (доставка покупателя
  переведена на дату начисления, наша доставка осталась по дате заказа и подмножеством: июль -214 тыс.);
  «сверка до рубля» за период, который страница не может показать.
- **Semantic второй раз подряд** (23.09 Маркет, 25.09 OZON): база налога и АДМ меняются в коде без правки
  knowledge/semantic/ym-rates-adm-tax.md. Проверять `git diff origin/main...HEAD -- knowledge/` при любой правке ставок.
- **Хук bash-scope: 3 ложных блока** (перенаправление в относительный путь после cd; `v>1` внутри python -c;
  `ln -s` из репозитория в /tmp принят за запись в цель). Обход прежний: абсолютные пути и Write скрипта.
