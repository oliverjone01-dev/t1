/* ============================================================================
   cms-config.js - подключение Google-таблицы к контент-системе.

   КАК ПОДКЛЮЧИТЬ (один раз):
   1. Открыть Google-таблицу (шаблон создаётся скриптом / см. README-cms.md).
   2. Доступ: «Настройки доступа» → «Все, у кого есть ссылка» → «Читатель».
      (gviz читает только публично-просматриваемые таблицы; ссылка нигде не
      публикуется - это внутренний инструмент.)
   3. Скопировать ID таблицы из её URL:
      docs.google.com/spreadsheets/d/<ВОТ_ЭТОТ_ID>/edit
   4. Вставить ID ниже в sheetId и запушить файл. Готово - сайт читает таблицу.

   Имена вкладок должны совпадать с rubricTab / planTab ниже (по умолчанию
   «Рубрикатор» и «Контент-план»). Колонки - как в README-cms.md.

   Пока sheetId = 'PASTE_SHEET_ID' - сайт работает на резервных данных
   (cms-seed.js) и честно показывает плашку «резервные данные».
   ========================================================================== */
window.GG_CMS_CONFIG = {
  sheetId: 'PASTE_SHEET_ID',
  rubricTab: 'Рубрикатор',
  planTab: 'Контент-план'
};
