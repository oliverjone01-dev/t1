# ФЕНИКС Step 12.5 - Контур, итерация 3

**Артефакт:** `kontur-dashboard/` + `.github/workflows/deploy-pages.yml` + `.github/workflows/kontur-snapshot.yml`
**Ветка:** `claude/kontur-dashboard`, PR #358, head `3098e5e`
**Класс:** mixed (дашборд с цифрами + гейт публикации + текст собственнику)
**Дата:** 2026-09-23
**Серия:** iter 1 `0b53ec2` veto 4.75 -> iter 2 `de019d7` return 6.00 -> **iter 3 `3098e5e` veto 5.55**

**anchor:** 5.83 veto Студия каруселей v4 - наш артефакт ниже на 0.28. Механика одна: слияние публикует
наружу артефакт, который показывает читателю не то, что лежит в данных. Ближайший сверху 6.5 return
редизайн annotate - наш ниже, потому что падает не оформление, а целый экран. Ближайший снизу
5.05 veto MASTER_SYSTEM_v9 iter 1 - наш выше, потому что блокирующий 2 закрыт по-настоящему.

**JSON-отчёт:** `knowledge/episodes/2026-09/feniks-audit-kontur-dashboard-iter3-20260923.json`
(`python3 schemas/validate.py audit-report` -> VALID; вектор весов пересчитан вручную, дельта 0.0000)

---

## Что проверялось и чем

Ограничения среды: сеть закрыта на `cdn.tailwindcss.com` и `cdnjs.cloudflare.com`, поэтому графики
в браузере не рисуются. Текст экранов это не затрагивает: я снимал `innerText` из `#view`, повторяя
сборку из `render()` вместе с блоками «Что смотреть» и «Метод и его границы». Боевое поведение
tailwind имитировал точечно, через `addStyleTag('.flex{display:flex}')`.

Рабочие файлы проб лежат в моём scratchpad:
`/tmp/claude-0/-home-user-t1/35b51466-2267-51bd-bef3-fa7175295f18/scratchpad/`
(`dump_screens.mjs`, `probe_bridge.mjs`, `probe_cards.mjs`, `probe_gate.mjs`, `probe_hidden.mjs`,
`gate_step.sh`, `probe_a6.sh`, `full_repo_test.sh`, `arith.py`, `screens.txt`).

---

## Три блокирующих пункта итерации 2: что с ними стало

| # | Пункт | Итог | Чем подтверждено |
|---|---|---|---|
| 1 | Рецидив «целей нет, конверсия остаётся гипотезой» | закрыт частично | дословной фразы нет ни на одном из 52x2 экранов; то же утверждение осталось на `onepage` и `yd-cost` по GENGLASS и на четырёх экранах GLASS-MEMORY |
| 2 | Два числа ДАННЫЕ по одной метрике | **закрыт** | 2 376 и 3 012 проверены из исходных файлов, плашка рендерится, коридор 4,78-6,06% воспроизведён |
| 3 | Модель против факта на «Мосте до денег» | **сломан** | текст написан верно и не рендерится: экран падает с TypeError, `node tools/smoke.mjs` -> exit 1 |

### Блокирующий 3: разбор

`public/index.html:610` в экране `mn-bridge`:

```js
const cv = (V().ym && V().ym.conv) ? V().ym.conv.organic : null;
```

`public/index.html:652` (из `src/modules/scr_a.py:122`), новая плашка:

```js
+ (cv? '<div class="mb-4">'+note('Модель визитов не сходится с тем, что видит счётчик',
     'Частотность '+nf(537874)+' ... '+nf(cv.organic.visits)+' визитов из поиска ...
```

`cv` здесь уже равно `conv.organic`, поэтому `cv.organic` это `undefined`. Выражение скопировано
с экрана «Цели и конверсии» (`scr_c.py:96`), где `cv = ym.conv` и обращение верное.

Живой прогон, `CUR='gg'`: `go('mn-bridge')` -> `TypeError: Cannot read properties of undefined
(reading 'visits')`, `viewLen=0`. По GLASS-MEMORY экран жив (`viewLen=1448`), потому что там `cv=null`
и плашка не строится. То есть падает ровно тот проект, ради которого правка делалась.

Следствие: в дампе отрендеренного текста 52 экранов x 2 проекта не встречается ни одна строка
блокирующего 3 - «Модель визитов не сходится» 0, «32 272» 0, «10.7 раза» 0, «0.56%» 0,
«значит итог тоже гипотеза» 0, «сверяет Спартак» 0. В `public/index.html` каждая из них есть по разу.

Арифметика самой плашки при этом верна, я пересчитал из файлов: `537874 × 0.06 = 32 272,44`,
`32 272,44 / 3 012 = 10,715` -> «10.7», `3 012 / 537 874 = 0,56%`. Работа сделана и не доехала.

---

## Red-team probes

| ID | Result | Коротко |
|---|---|---|
| A1 | FAIL | до пароля читаются plan 240 000 000, 97 имён целей CRM, conv 4.78 |
| A2 | FAIL | `sessionStorage` и `boot()` дают результат, идентичный верному паролю (viewLen 2697) |
| A3 | PASS | без секрета раздел не публикуется, `::warning::`, exit 0 |
| A4 | PARTIAL | неверный пароль блокирует; соли и ограничения попыток нет |
| A5 | PASS | хэш подставляется посимвольно верно |
| A6 | PASS | сломанный маркер -> `::error::`, exit 1, `_site/kontur` не создан; `continue-on-error` возвращён |
| A7 | FAIL | в сессии литерал `'1'`, срока действия и отзыва нет |
| B1 | PARTIAL | сторож чист, но 97 целей уехали в ГИПОТЕЗА, а «Отказы 19%» живёт вне конверта |
| B2 | PASS | 32 пары карточка-ряд, расхождений 0 |
| **B3** | **FAIL** | экран «Мост до денег» по GENGLASS не рисуется, `smoke.mjs` exit 1 |
| B4 | PASS | два числа визитов сведены, причина названа верно |
| B5 | PARTIAL | в одной среде сборка детерминирована; между средами по-прежнему разная |
| B6 | FAIL | `git_file_date` выбрасывает дату, которую git отдаёт |
| B7 | FAIL | наружу уезжает падающий P0-экран и четыре противоречащие данным поверхности |
| C1 | FAIL | одно состояние описано противоположно на разных экранах одного проекта |
| C2 | PASS | жаргон не вернулся |
| C3 | PASS | длинных тире 0 |
| C4 | PASS | Anti-Slop 0 совпадений по 27 шаблонам |
| C5 | PARTIAL | overclaim снят, регистр разработчика и имя агента остались |
| C6 | FAIL | точка и запятая как десятичный разделитель в одном документе |

---

## Evidence ledger

| # | Gap | Evidence | Чекпоинт | Вес |
|---|---|---|---|---|
| 1 | Экран «Мост до денег» по GENGLASS не рисуется | `public/index.html:610` против `:652`; `go('mn-bridge')` при `CUR='gg'` -> TypeError, `viewLen=0`; `node tools/smoke.mjs` exit 1; 6 ключевых строк правки в дампе экранов = 0, в `public/index.html` = 1 | insight_12 | -2 |
| 2 | «Одна страница собственнику» спорит с измеренной конверсией | `scr_d.py:82` против `conv.organic={v:4.78,k:ДАННЫЕ,visits:3012,leads:144}` и `goals_configured.v=97` | accuracy_3 | -2 |
| 3 | «Расход и CPL»: «лидов нет: цели в счётчике не настроены» | `scr_c.py:154` против `direct_metrika.json` `totals.ad_leads_30d=105`, `ad_visits_30d=4568`; CPL из имеющихся цифр 173 585 / 105 = 1 653 ₽ | accuracy_3 | -2 |
| 4 | Механизм рецидива не тронут: общий текст на два проекта | `build_data.py:356-363` (DB.blockers общий), `read.py:94-96` (RD статичен); на gm рендерится «две выгрузки дают разное число» и «Обе цифры на экране» там, где выгрузок нет | accuracy_3 | -1 |
| 5 | «Отказы 19%» против карточки ДАННЫЕ 17.1 на том же экране | `metrika.json` `bounce_rate=17.1`; `scr_c.py:92`, тело плашки правилось этим же коммитом | accuracy_1 | -1 |
| 6 | `git_file_date` выбрасывает дату, которую git отдаёт | `git rev-parse --is-shallow-repository` -> true, но `git log -1 --format=%cs -- yandex-direct/data/raw/metrika-goals.json` -> 2026-09-16; закоммичено `{v:97,k:ГИПОТЕЗА,at:''}`; песочница с полной историей даёт `{k:ДАННЫЕ,at:...}` | accuracy_2 | -1 |
| 7 | Плитка «Точек в ряду 41 · positions.ndjson» считает точку, которой в файле нет | `scr_dyn.py:136`; в `positions.ndjson` по gg последняя дата 2026-09-19 | accuracy_2 | -1 |
| 8 | В конвейере нет проверки, исполняющей экраны | `kontur-snapshot.yml:40-62` без шага `smoke.mjs`; `check_dash.py` exit 0 на коде, где `smoke.mjs` exit 1 | risk_23 | -2 |
| 9 | Дымовой тест смотрит не на боевой DOM | `smoke.mjs:13` инжектит `[hidden]{display:none!important}`, `grep -c '\[hidden\]' public/index.html` = 0; после `addStyleTag('.flex{display:flex}')` `#app` display flex при атрибуте hidden, bodyTextLen 1 040 | risk_23 | -1 |
| 10 | Действие по мосту недостижимо, без срока, ответственный - агент | `scr_a.py:127-130`, «до среднего чека, не после» не дата; «сверяет Спартак» в дампе экранов 0 раз | actionability_6 | -1 |
| 11 | Публикация без записанного решения о принятом риске | проба A1; в `kontur-dashboard/docs/` такого файла нет; прецедент `deploy-pages.yml:133-134` -> `gg-sales-academy/sealed/README.md` | brand_19 | -1 |
| 12 | Одна сущность двумя словами | дамп 52x2: «лид\*» 37, «заявк\*» 12, оба слова на четырёх экранах | brand_20 | -1 |

---

## Оценка

| Критерий | Вес | Чекпоинты | Score | Было iter 2 |
|---|---|---|---|---|
| Accuracy | 25% | 1+1+0+2+2 | 6.0 | 7.0 |
| Actionability | 25% | 1+1+1+1+0 | 4.0 | 5.0 |
| Insight | 20% | 2+1+2+2+0 | 7.0 | 6.0 |
| Brand Fit | 15% | 1+2+2+1+0 | 6.0 | 6.0 |
| Risk Awareness | 15% | 1+1+0+1+2 | 5.0 | 6.0 |

**weighted_total = 0.25×6.0 + 0.25×4.0 + 0.20×7.0 + 0.15×6.0 + 0.15×5.0 = 5.55**

**verdict: veto** (weighted < 6.0). Независимо от арифметики выше `return` вердикт подняться не мог:
FAIL по пробам класса A (A1, A2, A7) и по B7.

### Где я понизил оценку за свою же ошибку калибровки

`accuracy_1` опущен с 2 до 1 и `insight_12` поднят с 0 до 1. Первое - не регресс автора: числа
«Отказы 19%», «547 запросов», «561 запрос» рендерились и в итерации 2, я их не нашёл, потому что
не прогонял прозу экранов на числа вне конвертов. Пишу это прямо, чтобы автор не искал, что он
сломал в этих местах. Второе - честный плюс: второй порядок по мосту действительно разобран.

Снижение 6.00 -> 5.55 целиком объясняется падением экрана. Контрфактический расчёт: без падения
actionability ~5.5, insight 8.0, risk 6.0, weighted ~6.28, то есть `return`, а не `go`.

---

## Что блокирует слияние прямо сейчас

1. **Падающий P0-экран.** Собственник, открывающий «Деньги -> Мост до денег» по GENGLASS,
   видит белое поле. Правка на три обращения в одной строке.
2. **Два P0-экрана, спорящие с данными** (`onepage`, `yd-cost`), и тот же механизм на GLASS-MEMORY.
3. **Отсутствие в конвейере проверки, которая исполняет экраны.** Без неё `kontur-snapshot.yml`
   будет коммитить сломанную страницу в `main` по будням, и следующий такой дефект уедет тем же путём.

## Что не блокирует, вопреки ожиданию

- **п.8 прошлого отчёта** (дымовой тест смотрит не на боевой DOM) сам по себе слияние не блокирует:
  он не меняет того, что видит пользователь после ввода пароля. Но он объясняет, почему падение
  экрана не было поймано раньше, поэтому идёт в один пакет с пунктом 3 выше.
- **п.10** (файл с записанным решением о принятом риске публикации) не блокирует техническое слияние
  и не может быть закрыт автором: решение принимает Иван. Отказ автора писать этот файл за Ивана
  считаю правильным, а не дефектом. Пока решения нет, пробы A1 и A2 останутся FAIL при любом коде,
  и вердикт выше `return` не поднимется даже после закрытия всех тринадцати пунктов rework_tz.
- **`accuracy_3` в self-check автора** («не проверял, тот же ноль, что в двух прошлых итерациях»):
  он не ноль по инерции. Он ноль потому, что есть четыре воспроизводимые поверхности, где артефакт
  спорит со своими же данными. Список выше, каждая с файлом и строкой.

---

## Эскалация

`verdict: veto` при weighted 5.55. По CLAUDE.md §9 это остановка deliver и эскалация Ивану;
порог остановки всей ветки (<5.0) не достигнут, работа по ветке продолжается.

Итерация 3 исчерпана (`phoenix-eval`: return max 3 iterations). Решение о том, идти ли на четвёртый
заход или закрывать PR иначе, за Иваном. Моя рекомендация: три блокирующих пункта выше правятся
одним коммитом за час, критерий приёмки по каждому проверяется одной командой, и после этого
вердикт `return` с коротким хвостом реалистичен. `go` при живых A1 и A2 невозможен без решения Ивана
о принятом риске публикации.

---

## Соблюдение Hard Rule 4 (ФЕНИКС не пишет продукт)

`git status --porcelain` на момент сдачи отчёта: только `traces/2026-09-23/agents.jsonl` (моя зона)
и два новых файла в `knowledge/episodes/2026-09/` (моя зона). В `kontur-dashboard/`, `.github/`,
`schemas/` я не писал ничего. Все пробы шли на копиях в `/tmp`.

Побочное наблюдение по моим же хукам: `feniks-bash-scope.sh` дважды дал ложное срабатывание на
`sed -i "s|...|...|"` по файлу внутри `/tmp` (принял тело sed-скрипта за путь). Это не влияет на
аудит Контура, но стоит строки в Protocol 15.
