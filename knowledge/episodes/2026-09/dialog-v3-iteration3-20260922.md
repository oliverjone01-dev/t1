# Диалоги, итерация 3: гейт ball на hotOpen + точность RE_HOT (методика + self-check)

Артефакт-класс: гейт-инструмент / дашборд, влияет на оценку менеджеров (Protocol 6 HITL).
Владелец решения: Иван. Скоуп: только дашборд Диалоги, РОП-дашборд не трогаем.
Коммит: `02fee94` в `dialog-export-v1` (локально, не запушен до вердикта ФЕНИКСА).
База сравнения: `cecc4710` (ФЕНИКС go 8.4). Снимок dialog.json 2026-09-21, rop.json 2026-09-22T08:03.

## Что закрывали
Остаточные gaps из аудита go 8.4, помеченные ФЕНИКСОМ как не-блокеры (gap 1 и gap 3).
Gap 2 (калибровка веса hot_slow) по календарю не наступил, gap 4 (чистые веса из calib-log) тоже.

## Решение 1 (gap 1): двойной штраф ball + hot_slow
[ДАННЫЕ] На cecc4710 оба фактора стоят на 320 сделках, бюджет 17.7 млн руб, множитель 0.6x0.7.
ФЕНИКС предложил `&& !hotSlow` или min. Оба варианта замерены, взят более точный.

Разложение пересечения [ДАННЫЕ, инструментированный прогон]:
- 271 сделка: горячая реплика вообще без ответа. Тогда она же и есть текущий «мяч»,
  физически одна задержка. Штрафовать дважды нельзя.
- 49 сделок (6.1 млн руб): горячую реплику ответили позже 4 раб.ч, потом клиент написал снова
  и ждёт сейчас. Это два разных события и два разных дефекта.

Реализовано: `hotOpen` (есть горячая реплика без ответа вовсе), гейт `ball && !hotOpen`.
Гейт на `hotSlow` снял бы и 49 законных случаев. При hotOpen остаётся более сильный штраф 0.6.

Проверка края: свежая горячая реплика без ответа моложе 4 раб.ч даёт hotSlow всего на 1 сделке
из 5036, то есть ветка `!nxt` практически не создаёт ложных срабатываний. Не трогали.

## Решение 2 (gap 3): точность RE_HOT
ФЕНИКС указал на широкое «есть ли». Замер по альтернативам показал, что оно не самое грубое.
[ДАННЫЕ] сделок, где альтернатива была единственным триггером hot_slow (всего 461 сделка):
`сч[её]т` 150, `оплат` 63, `договор` 27, `есть ли` 23, `в наличии` 19, `реквизит` 12, `отгруз` 7.

- «есть ли» убрано. Живые реплики из выборки: «есть ли живые фото или видео товара»,
  «есть ли где шоу рум посмотреть стол», «Есть ли какие-то скидки для физ. лиц»,
  «есть ли возможность сделать по моей ширине». Это не запрос о наличии товара.
  Наличие продолжают ловить «в наличии» и «на складе».
- `сч[её]т` закрыт от подстроки и от идиомы: `(?<!за )(?<![а-яё])сч[её]т`. Ловил «жду ра-счет-ы»,
  «заказ на расчет», «за счет чего можно уменьшить стоимость». Расчёт в GENGROUP это стадия
  воронки и ежедневное слово, а не просьба выставить счёт. Реальные просьбы остаются:
  «Прошу выставить счёт или КП», «сделайте пожалуйста счет на эту позицию», «Счет 1638».

Это тот же класс дефекта, что ФЕНИКС снял с client_chase в раунде 1 (L1, ложное обвинение).
Расширение регулярки сознательно не делали: `срок[аи]\b` мертва (в JS `\b` не работает после
кириллицы, 0 срабатываний), её починка добавила бы штрафов. Сужение и расширение в одном
проходе не смешиваем, починка вынесена в бэклог отдельным решением.

## Решение 3 (gap 3, framing)
Тон метки привязан к силе улики, как у client_chase: не ответили совсем - «Горячая реплика
без ответа», tone bad; ответили позже 4 раб.ч - «Поздний ответ на горячую реплику», tone warn,
клиент ответ всё же получил. Раньше оба случая были bad с одним текстом.

## Замер эффекта [ДАННЫЕ]
Базовый прогон на неизменённом коде воспроизводит закоммиченный dialog.json cecc4710
байт-в-байт (md5 b52e60f5...), повторный прогон после правки идемпотентен (md5 a58a4a99...),
то есть весь диф ниже объясняется только этой правкой.

| фактор | было | стало |
|---|---|---|
| ball | 1367 | 1144 |
| hot_slow | 461 | 353 |
| hot_fast | 323 | 251 |
| slow_resp | 274 | 293 |
| client_chase | 403 | 403 |

- Шанс: вырос на 306 сделках, упал на 130, у 4600 без изменений. Средний сдвиг по затронутым
  2.8 п.п., максимум +42 п.п. и -13 п.п. Бюджет затронутых 30.7 млн руб.
- Метки «Разметка»: 601 -> 463 (264 bad + 199 warn). Снято 138 ложных, 199 переклассифицировано.
- Рейтинги менеджеров, проценты по 6 разделам регламента и состав очередей РОПа не меняются:
  тон `mark()` пишется в evTags (колонка «Разметка»), а секционные проценты считаются по `tags`.
  Это проверено сравнением обоих артефактов, не рассуждением.

Максимальные сдвиги вверх (+42, +39, +37) - это сделки, где снят ложный hot_slow и на его месте
встал hot_fast: множитель меняется 0.6 -> 1.2. Развилка if/else между hot_slow и hot_fast
существовала до этой правки и не менялась. Если ФЕНИКС сочтёт скачок 2x чрезмерным,
это отдельное решение по демпфированию развилки, а не часть этой итерации.

## Downside и обратимость
[ГИПОТЕЗА] Направление правки - снятие штрафов, поэтому риск смещён в сторону мягкости:
менеджер, реально затянувший ответ на «есть ли фото», больше не получает метку. Цена ошибки
здесь ниже, чем у ложного обвинения (тот же приоритет, что ФЕНИКС применил к client_chase L1).
Downside при -50% полезности: часть настоящих горячих запросов о наличии без слов
«в наличии»/«на складе» перестанет попадать в hot_slow. Ловится по FP/FN-разбору на приёмке.
Rollback: `git revert 02fee94` в `dialog-export-v1` плюс пересборка. Side-effect на
calib-log.ndjson и trend.json - перезапись дня 2026-09-21 новым определением факторов;
для варианта B это делает лог однородным по определению, а не смешанным.

## Приёмка
Владелец Иван. Метрика: доля ложных меток «горячая реплика» при выборочном разборе 20 сделок
из очереди «Ждут ответа», плюс FP-rate по calib-log. Дата: вместе с приёмкой item 2 (+2 недели
от выкладки v2), отдельного срока не заводим, чтобы не плодить проверки.

## Self-check phoenix-eval (25 чекпоинтов; да / частично / нет)
Accuracy: 1 цифры тегированы [ДАННЫЕ]/[ГИПОТЕЗА] - да; 2 источники воспроизводимы (снимок,
байт-в-байт базовый прогон, команда в методичке) - да; 3 конфликта с RAG нет - да;
4 терминология глоссария v2.1 - да; 5 бренды - н/п.
Actionability: 6 владелец приёмки (Иван) - да; 7 дедлайн (+2 нед, вместе с item 2) - да;
8 ресурсы (снимки, лог, worktree) - да; 9 метрика успеха (FP-rate по 20 сделкам) - частично,
порог «сколько ложных допустимо» не зафиксирован; 10 milestone - да.
Insight: 11 нетривиально (разделение hotOpen и hotAnsSlow вместо тупого гейта) - да;
12 второй порядок (slow_resp разгейчивается на 19 сделках, это учтено в замере) - да;
13 альтернативы (`!hotSlow`, min, `!hotOpen`) замерены, не обсуждены на словах - да;
14 anti-median - да; 15 cross-domain (precision/recall, цена FP против FN) - частично.
Brand fit: 16 голос - н/п (внутренний инструмент); 17 anti-slop - да; 18 em dash - нет em dash;
19 output routing (дашборд) - да; 20 читаемость для РОПа (тексты меток стали точнее) - да.
Risk: 21 downside при -50% - да; 22 P9 hard rules (каждая цифра с источником, гипотезы помечены) - да;
23 crisis - н/п; 24 зависимости (regex по телу реплики, транскриптов звонков нет, учтено) - да;
25 обратимость (git revert, ветка не запушена) - да.

## Не сделано осознанно
- Починка мёртвой `срок[аи]\b`: это расширение штрафов, отдельное решение Ивана.
- Демпфирование развилки hot_slow / hot_fast (скачок множителя 0.6 -> 1.2): существовало до
  правки, в скоуп итерации не входило.
- Пересчёт BASE_RATES (висит с прошлого раунда как B6 PARTIAL, влияние малое).
