# GG-MarkPlan 2026

Отдельный внутренний сайт GENGROUP - **единый маркетинговый план 2026** по 5 брендам
(GENGLASS, VALONTI, GENTERO, Metal-GM, GLASS-MEMORY), приоритет GENGLASS.

Не дашборд аналитики и не SEO-хаб. Это слой **планирования**: цели года → инициативы
по кварталам → каналы → бюджеты и KPI, с явным разделением `[ДАННЫЕ]` и `[ГИПОТЕЗА]`
(Protocol 9) и гейтом ФЕНИКСА (Step 12.5) перед фиксацией цифр как факта.

Владелец домена задачи: **marco** (CMO, контент- и канальная стратегия).
Цифры и источники: **data** (Protocol 9). Финансовая вменяемость бюджетов и ROMI:
**roman** (CFO). Гейт перед публикацией плана как решения: **feniks** (Step 12.5).

## Зачем это отдельный проект, а не папка в аналитике

- **Другой горизонт.** Аналитика (`analytics-mvp/`) отвечает на вопрос «что было вчера»
  (P&L, воронка OZON по дням). MarkPlan отвечает «что делаем в 2026 и зачем» - кварталы,
  ставки, последовательность. Разные циклы: план меряется кварталами, аналитика - днями.
- **Другая аудитория.** Аналитика - для коммерческого блока. MarkPlan - для CMO, владельцев
  брендов и ФЕНИКСА: где ставки, чем обоснованы, кто и когда проверит первый сигнал.
- **Свой раздел на сайте.** Публикуется изолированной статикой в `/markplan/` на общем
  GitHub Pages GENGROUP (см. `deploy-pages.yml`), не мешаясь в аналитику. Переезд на
  отдельный домен - позже, сменой базового URL, без переписывания.

## Дисциплина Protocol 9 (встроена в продукт)

Планирование - главный источник розовых очков, поэтому правила зашиты в интерфейс:

- Каждая цифра в плане несёт метку **`[ДАННЫЕ]`** (со ссылкой на источник) или
  **`[ГИПОТЕЗА]`** (со списком допущений). Немаркированных чисел в плане не бывает.
- Сид-данные этого репозитория - **скелет-заглушка** (`status: skeleton`). Это каркас
  разделов, а не утверждённый план. Баннер в шапке предупреждает об этом явно.
- Инициатива не переходит в статус `committed`, пока не пройдёт триаду ДАТА → ФЕНИКС → МАРКО
  и не получит владельца и дату первого сигнала (Q5 Reality Audit).
- Бюджет >200K на чистой гипотезе, ROMI >50x без unit-эк, диапазон оценки шире 2x -
  блокируются на входе (Hard Rules CLAUDE.md §5), инициатива помечается флагом.

## Что внутри (модули)

| Модуль | Статус | Что делает |
|---|---|---|
| **План года** | скелет | Цели 2026 по холдингу и по каждому бренду, статус ставок |
| **Кварталы (Q1-Q4)** | скелет | Инициативы по кварталам: владелец, статус, ICE, метка P9 |
| **Каналы** | скелет | Микс каналов на бренд (Директ, SEO/GEO, SMM, МП, PR), связь с инициативами |
| **Бюджет и KPI** | скелет | Плановые бюджеты и целевые KPI - только как `[ГИПОТЕЗА]` до гейта ROMAN + ФЕНИКС |
| **Гейты** | план | Лог прохождения Step 12.5 по каждой committed-инициативе |

## Структура папки

```
GG-markplan-2026/
  README.md                        # этот файл - charter проекта
  ROADMAP.md                       # этапы сборки, Kill Criteria
  DECISIONS.md                     # решения автономной сборки MarkPlan
  BACKLOG.md                       # что дальше, по приоритету
  plan-motivaciya-prioritety.md    # план мотивации/приоритетов (после ФЕНИКС-аудита)
  plan-motivaciya-prioritety.csv   # тот же план задачами - под вставку в таблицу
  playbook-progrev-dizainera.md    # плейбук воронки прогрева дизайнера
  public/                          # приложение (статический сайт, без сборки)
    index.html                     # хаб-скелет (Обзор/Год/Кварталы/Каналы/Бюджет)
    app.css / app.js               # ядро хаба
    plan.html                      # ФЛАГМАН: сайт плана мотивации + Гант
    plan.css / plan.js             # авторская тема «чертёж» + SVG-диаграмма Ганта
    plan-data.js                   # встроенный снимок плана (фолбэк/дефолт)
    data/
      plan-2026.json               # скелет хаба
      gantt-import.csv             # вкладка GANTT для привязки живой таблицы
```

## Сайт плана (`plan.html`) и привязка к Google-таблице

`plan.html` - авторская страница плана: герой, развилка, модель 15 млн/мес,
диаграмма Ганта с зависимостями (плавные кривые, карточка при наведении, подсветка
цепочки, выходные-ритм, гейты, линия «сегодня»), блоки задач, воронка прогрева,
открытые решения. Тёмная тема, шрифт SF Pro Display, кроссбраузерно и адаптивно
(проверено на 360-1300px, Playwright, 0 JS-ошибок, без горизонтального вылета).

**Данные Ганта - живые из Google-таблицы, с фолбэком на встроенный снимок.**
Логика в `plan.js`: сначала пробуем таблицу, при неудаче показываем снимок
(`plan-data.js`). Бейдж в шапке показывает источник («живая таблица» / «снимок»).

Привязанная таблица (создана на интегрированном аккаунте):
`https://docs.google.com/spreadsheets/d/155GvseJd5QUlIujIj1AgZa1no1SvDxlcmcNtCOQKvSA/edit`
(id прописан в `plan-data.js` → `meta.sheetId`).

Как включить автосинк (меняете таблицу - меняется график и блоки на сайте):

1. Открыть таблицу по ссылке выше, залить в неё полный план из
   `public/data/gantt-import.csv` (Файл → Импорт → заменить лист). Колонки:
   `ID, Блок, Задача, Ответственный, Старт, Дней, Зависит, Статус, Гейт`. Старт -
   дата, Дней - число, Зависит - ID через точку с запятой, Статус -
   `в работе / план / к обсуждению / готово`.
2. Сделать таблицу читаемой сайтом - **один клик**: Файл → Поделиться →
   Опубликовать в интернете → лист → **CSV** → Опубликовать. Скопировать URL вида
   `.../pub?gid=0&single=true&output=csv`.
3. Вписать этот URL в `plan.js` → `CONFIG.csvUrl` (приоритетнее gviz, надёжен по CORS).
   После этого бейдж переключится на «живая таблица», а правки в Google-таблице
   будут подтягиваться при перезагрузке страницы.

Альтернатива без публикации: сделать таблицу «Доступ по ссылке: читатель» - тогда
сработает gviz по `meta.sheetId` (но публикация в CSV надёжнее по CORS).

Если живая таблица недоступна (не заполнена, не опубликована, оффлайн) - сайт не
падает, показывает встроенный снимок. Снимок правится в `public/plan-data.js`.

## Локальный просмотр

Сборки нет - чистая статика. Любой статик-сервер из `public/`:

```bash
cd GG-markplan-2026/public && python3 -m http.server 8080
# открыть http://localhost:8080
```

## Хостинг

Публикация в подпапку `/markplan/` общего сайта GENGROUP через `deploy-pages.yml`
(триггер на `GG-markplan-2026/**`). Отдельный домен - позже, отдельным решением;
запуск не блокирует.

---

**Версия:** v0.1.0 (скелет)
**Владелец:** Иван Раюшкин (CMO) · домен задачи - marco
**Гейт:** ФЕНИКС Step 12.5 обязателен перед переводом любой цифры плана в `[ДАННЫЕ]`/`committed`
